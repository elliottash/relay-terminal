<!-- relay:entry 20260926T034227Z-ph author=agent kind=event model=claude-opus-5-5 pane=269e2839 turn=6173581165764472b6c340786cfdcc26/5cf2d2908f984b848503f12052a6d41f -->
- ✦ agent created this card in Inbox · .board/changes/2026-09-25-models-pane-shows-one-pane-s-model-as-current-an.md

<!-- relay:entry 20260926T034242Z-yd author=agent kind=evidence model=claude-opus-5-5 pane=269e2839 turn=6173581165764472b6c340786cfdcc26/5cf2d2908f984b848503f12052a6d41f -->
Trace (2026-09-25, read-only; no routing code changed).

**Where "· current" comes from.** `src/ModelsPane.cpp:246` copies the serving pane's `Target::currentKey`/`currentEffort` into the picker context; `Pane::currentEntryKey()` (`src/Pane.h`, ~2596) derives it from that pane's `m_currentPreset` + `paneModel()`. The picker appends "· current" on Enabled, Pick order and Effort. Those three tabs edit global lists, so a per-pane marker there is wrong.

**How Kimi got there with no manual pick.** `relay.conf` Main list has four rank-1 entries (glm-coding|glm-5.3, guest:claude|opus, kimi-code|k3, guest:codex|gpt-6-sol). `routing-draws.jsonl` at 02:31:15Z (10:31:15 pm EDT) pane 6ad44757: rank-1 draw chose `kimi-code|k3` (p≈0.26); `relay.log` then shows `configure_sent pane=6ad44757 preset=kimi-code model=k3` and no `model_pick`. Same for c91dfb9a (02:28:36Z) and 76185c26 (02:39:03Z). So Kimi was a legitimate random draw for that pane, which the global tabs then labelled "current". The screenshot does not name its pane, so which one is shown is unproven.

**Paths that pick a model, and whether they follow "Main/High/Flash, random within rank 1":**

| Path | Code | Behaviour | Follows rule? |
|---|---|---|---|
| New pane | `startEntry` → `drawTier("main")`, `src/ModelCatalog.cpp:1429/1455`; `src/PaneEvents.cpp:297` | quota-weighted draw among usable Main rank 1 | yes |
| Switchboard/helper start | `src/RelayWindow.h:5338` | same `startEntry` draw | yes |
| Restored pane | `startEntry` restored branch, `ModelCatalog.cpp:1435` | reuses saved preset/model before any draw | no (sticky) |
| Mode switch High/Flash | `backend/worker.py:817` → `choose_role` → `ordered_candidates(choose=True)` | draws, but the result is stored as a pane mode pick (`Pane::setAgentRole`, `m_modePick`) and reused on later switches | first time only |
| Main via `_tier("main")` | `backend/relay_core/roles.py` `_tier` | returns the pane's own model; the Main list is only a failover order | no |
| Mode labels / previews | `src/ModelRows.cpp:~175`, worker `summary()`/`tier_summary()` | first usable row, no draw | no |
| `/swap` | `swapTarget` → `mainDefault`, `ModelCatalog.cpp:1463` | first usable Main row | no |
| Profile applied status | `src/Pane.cpp:167` `mainDefault` | names first row as "main runs on" | no |
| No Main list / catalog empty | fallback to `provider/preset` in `relay.conf` (written on every configure) | last-configured provider | no |

**Fix direction (to agree).** 1) Drop "· current" (and any per-pane state) from Enabled, Pick order and Effort; if pane state is shown anywhere, show it only in a clearly per-pane view. 2) One draw function for every choosing path (restore excepted only if the owner wants restored panes sticky), used by `/swap`, mode switches, and the no-list fallback; labels say "random at rank 1 among …" instead of naming the first row. 3) Stop writing `provider/preset` as an implicit global default.

<!-- relay:entry 20260926T034242Z-ye author=agent kind=event model=claude-opus-5-5 pane=269e2839 turn=6173581165764472b6c340786cfdcc26/5cf2d2908f984b848503f12052a6d41f -->
- ✦ agent moved this card · Inbox → Discussing · the discussion started

<!-- relay:entry 20260926T034531Z-ye author=agent kind=event model=claude-opus-5-5 pane=cae84571 turn=258ff0e9de5540e09f638ed1541c9168/978abc26046f49f194a85b9aefacbda7 -->
- ✦ agent claimed this card · assignee agent, Discussing → Running, implemented_by anthropic/claude-opus-5-5 via claude:ashe-ethz-ch, session cae84571

<!-- relay:entry 20260926T034531Z-yf author=agent kind=progress model=claude-opus-5-5 pane=cae84571 turn=258ff0e9de5540e09f638ed1541c9168/978abc26046f49f194a85b9aefacbda7 pane_token=cae84571-aa12-45d5-b3a2-881fb1298087 -->
Claimed (cae84571) · working on it from a terminal pane

Picking up from closed pane 269e2839: trace is on the thread; next is agreeing the fix direction with the owner, then removing hosted-tab "· current" and unifying the rank-1 draw.
