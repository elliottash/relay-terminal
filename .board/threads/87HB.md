<!-- relay:entry 20260920T035932Z-hr author=owner kind=event pane=switchboard -->
- ✦ owner flagged this card · priority -1

<!-- relay:entry 20260925T051103Z-1r author=owner kind=comment mode=discuss -->
Compare this to the recent discussion on mosh and potentially merge the cards

<!-- relay:entry 20260925T051103Z-wg author=owner kind=event pane=switchboard -->
- ✦ owner moved this card · Inbox → Discussing · the discussion started

<!-- relay:entry 20260925T051242Z-86 author=agent kind=event model=glm-5.3 pane=switchboard turn=7e754d612668112cbf1a0c6e7b75c2e8/dc81dd109ae245fc941f775062554deb -->
- ✦ agent updated this card · links: {"plans": ["docs/SSH-AND-MOSH.md"], "commits": [], "evidence": [], "related": ["… → {"commits": [], "evidence": [], "github": null, "plans": ["docs/SSH-AND-MOSH.md"…; waiting_on: (unset) → owner; title: The parts of tmux worth having in Relay itself → The local half of the tmux gap: a shell that survives Relay quitting; replaced `## What tmux is actually used for, and where Relay stands`

<!-- relay:entry 20260925T051242Z-mf author=agent kind=rewrite model=glm-5.3 pane=switchboard turn=7e754d612668112cbf1a0c6e7b75c2e8/dc81dd109ae245fc941f775062554deb -->
- ✦ rewrote title

<details><summary>before</summary>

```
The parts of tmux worth having in Relay itself
```

</details>

<details><summary>after</summary>

```
The local half of the tmux gap: a shell that survives Relay quitting
```

</details>

<!-- relay:entry 20260925T051242Z-mg author=agent kind=rewrite model=glm-5.3 pane=switchboard turn=7e754d612668112cbf1a0c6e7b75c2e8/dc81dd109ae245fc941f775062554deb -->
- ✦ rewrote ## What tmux is actually used for, and where Relay stands

<details><summary>before</summary>

```
| What people use tmux for | Relay today |
|---|---|
| Splits and tabs | Has them, natively, with the mouse and the keyboard. Nothing to add. |
| Scrollback, search, copy mode | Has them, with clickable paths and folds on top. |
| Naming and switching sessions | Tabs and panes have titles; conversations have their own list. |
| **A shell that survives the client dying** (network drop, crash, quit, reboot of the laptop but not the host) | **Missing.** A pane's shell is a child of Relay: when Relay exits or crashes, every running command dies with it. The layout, the directory and the scrollback text come back (`src/WindowState.h`); the processes do not. |
| **Detach here, attach there** | Partly: a pane can be shared with a paired phone (`remote/`), but there is no "leave it running and pick it up from another machine". |
| **On a remote host: keep a long job running across a disconnect** | Missing, and it is the reason most people type `tmux` after `ssh` at all. mosh (#S5SH) survives a *network* drop but not a client restart. |
| Pair programming in one session | The phone share covers watching; two desktops do not share a pane. |

So the gap is one thing, in two places: **a shell that outlives the window**, locally and on the host.
```

</details>

<details><summary>after</summary>

```
Compared against the mosh work on 2026-09-25 (owner's ask). Splits, tabs, scrollback, search and
session naming Relay already has — the gap was always **a shell that outlives the window**, in two
places, and one of the two is now built:

- **Remote: delivered, awaiting verification in #XQ8F.** Every ssh pane is persistent by default —
  a bare `tmux -L relay` holder on the host (screen fallback, plain shell last), invisible chrome,
  session named per pane, re-attached after a Relay restart, Ctrl+E splits on the host, mosh link
  optional. That is option 3 below, which this card called "the cheap experiment" — it shipped.
  #VD2M tier A (zellij inside Relay) and #234Z (Alt+Esc leaves a session) sit beside it.
- **Local: still missing, and what this card now holds.** A pane's shell is a child of Relay: when
  Relay exits or crashes, every local command dies with it. The layout, directory and scrollback
  text come back (`src/WindowState.h`); the processes do not. #XQ8F's holder runs on hosts only.
- **Detach here, attach there between two desktops**: still open. The phone share covers watching;
  two desktops cannot share a pane.
```

</details>

<!-- relay:entry 20260925T051303Z-sf author=agent kind=event model=glm-5.3 pane=switchboard turn=7e754d612668112cbf1a0c6e7b75c2e8/dc81dd109ae245fc941f775062554deb -->
- ✦ agent updated this card · replaced `## Options`

<!-- relay:entry 20260925T051303Z-sg author=agent kind=rewrite model=glm-5.3 pane=switchboard turn=7e754d612668112cbf1a0c6e7b75c2e8/dc81dd109ae245fc941f775062554deb -->
- ✦ rewrote ## Options

<details><summary>before</summary>

```
1. **A local session keeper.** Relay stops owning the pty directly: each pane's shell is started by a
   small helper (one process per pane, or one daemon for all of them) that owns the pty and keeps a
   ring buffer of output. Relay attaches over a Unix socket in `$XDG_RUNTIME_DIR`. Quitting Relay
   leaves the helper running (by choice, per pane or per tab); starting Relay offers to re-attach,
   with the live processes still there. A crash becomes recoverable instead of destructive.
   - Fits what is already built: `engine/pty/` is where the pty lives, the layout restore already
     knows which panes existed, and the scrollback restore already replays text.
   - Costs: a second process to ship and version, a protocol between it and the GUI, and careful
     handling of the terminal size and of orphaned helpers.
2. **The same keeper on the host.** With #S5SH the pane knows which host it is on and can run
   commands there over the user's own connection. The keeper binary could be copied to the host once
   (like Warp's and VS Code's remote servers) so a remote shell survives a disconnect without tmux.
   Bigger: per-architecture builds, versions, and a decision about installing anything on a host.
3. **Use an existing keeper instead of writing one**: `abduco`, `dtach`, or tmux itself in a single
   session, hidden behind Relay's own UI. Least code, and it works on hosts that already have tmux;
   but it is a dependency the user must have, and Warp's tmux experiment is a warning about hiding
   one behind your own UI (docs/SSH-AND-MOSH.md, "Research notes").
4. **`tmux -CC` control mode**: talk tmux's control protocol and map its windows and panes onto
   Relay's splits, the way iTerm2 does. Gives persistence and remote persistence with no new binary,
   but it is a large state machine and only helps people who already run tmux.

A sensible order is 1, then 2 or 4 once 1 has proved the attach/detach model. 3 is the cheap
experiment if the owner wants it working this week on hosts that have tmux.
```

</details>

<details><summary>after</summary>

```
1. **A local session keeper.** Relay stops owning the pty directly: each pane's shell is started by a
   small helper (one process per pane, or one daemon for all of them) that owns the pty and keeps a
   ring buffer of output. Relay attaches over a Unix socket in `$XDG_RUNTIME_DIR`. Quitting Relay
   leaves the helper running (by choice, per pane or per tab); starting Relay offers to re-attach,
   with the live processes still there. A crash becomes recoverable instead of destructive.
   - Fits what is already built: `engine/pty/` is where the pty lives, the layout restore already
     knows which panes existed, and the scrollback restore already replays text.
   - Costs: a second process to ship and version, a protocol between it and the GUI, and careful
     handling of the terminal size and of orphaned helpers.
   - **Cheaper now (2026-09-25): #XQ8F proved the attach/detach model with a tmux holder. A local
     pane could run its shell under the same `tmux -L relay` holder locally — no bespoke daemon,
     no new protocol — trading the tmux dependency for a week of work instead of a quarter.**
2. **The same keeper on the host.** ~~Open~~ **Rejected in favour of 3** (2026-09-25): per-architecture
   builds and installing anything on a host, which the mosh discussion set aside — #XQ8F's holder
   uses the host's own tmux/screen and installs nothing.
3. **Use an existing keeper instead of writing one** — **shipped for the remote case in #XQ8F**
   (tmux on a Relay socket, invisible; screen fallback; nothing installed). The original warning
   (Warp's tmux experiment, docs/SSH-AND-MOSH.md "Research notes") was answered by hiding all
   multiplexer chrome and naming sessions after panes, not windows.
4. **`tmux -CC` control mode** — **stays rejected**: a large state machine that only helps people
   who already run tmux (Research notes, same doc).
```

</details>

<!-- relay:entry 20260925T051310Z-hm author=agent kind=event model=glm-5.3 pane=switchboard turn=7e754d612668112cbf1a0c6e7b75c2e8/dc81dd109ae245fc941f775062554deb -->
- ✦ agent updated this card · replaced `## Open questions for the owner`

<!-- relay:entry 20260925T051310Z-hn author=agent kind=rewrite model=glm-5.3 pane=switchboard turn=7e754d612668112cbf1a0c6e7b75c2e8/dc81dd109ae245fc941f775062554deb -->
- ✦ rewrote ## Open questions for the owner

<details><summary>before</summary>

```
- Is "quit Relay, come back, the build is still running" worth a helper process in the product?
- Should re-attaching be automatic, or offered ("3 panes are still running")?
- Does the remote case (option 2) matter, or is mosh plus a long-running job with `nohup` enough?
```

</details>

<details><summary>after</summary>

```
- Is "quit Relay, come back, the local build is still running" worth having, now the remote half
  exists? (The third question below — does the remote case matter — was answered by #XQ8F: yes, and
  it shipped.) If yes, local holder (option 1 via #XQ8F's tmux) or nothing at all?
- Should re-attaching be automatic, or offered ("3 panes are still running")? #XQ8F chose offered:
  the restored queue waits for one keypress, never runs on its own.
- Detach here, attach there **between two desktops** — wanted, or is phone-sharing enough?
```

</details>

<!-- relay:entry 20260925T051326Z-nv author=agent kind=decision model=glm-5.3 pane=switchboard turn=7e754d612668112cbf1a0c6e7b75c2e8/dc81dd109ae245fc941f775062554deb -->
2026-09-25, owner: "Compare this to the recent discussion on mosh and potentially merge the cards." → Compared with #XQ8F/#VD2M. The remote half of this card — a session on the host that survives the link, the pane and a Relay restart — is delivered and awaiting verification in #XQ8F (tmux/screen holder on the host, nothing installed), with #VD2M tier A and #234Z beside it. Not merged: both are implemented cards in verification, and this card still holds one undelivered ask — local persistence. Card narrowed to that; options 2 and 4 recorded as rejected, option 3 as shipped for the remote case.

<!-- relay:entry 20260925T051333Z-f6 author=agent kind=question model=glm-5.3 pane=switchboard turn=7e754d612668112cbf1a0c6e7b75c2e8/dc81dd109ae245fc941f775062554deb -->
Now that remote panes are persistent by default (#XQ8F), is the **local** half still wanted — quit Relay, come back, the local build still running? Recommendation: yes, but as "run local panes under the same tmux holder #XQ8F proved" (small, no new binary to write), not the bespoke daemon this card originally sketched; and only if you find yourself wanting it. If you never miss it locally, close this card as delivered-remotely and we keep nothing on the books.

<!-- relay:entry 20260925T051344Z-nm author=agent kind=comment mode=discuss model=glm-5.3 turn=7e754d612668112cbf1a0c6e7b75c2e8/dc81dd109ae245fc941f775062554deb -->
I'll read the two related cards (#S5SH mosh, #SPBN) and the mosh design doc to compare them properly.

Both related cards read. #SPBN is only about visual pane styling — not a merge candidate. Let me check for any other mosh/persistence discussion and the research notes the card cites.

The recent mosh work is clear: **#XQ8F** (persistent remote panes) and **#VD2M** (zellij, mosh+zellij as the persistent session), both from 2026-09-24 and sitting in needs-verification. Reading both before judging the merge.

The picture is complete. One more read to get #87HB's hash so I can update it.

Full picture in hand. **My call: don't merge — narrow.** The mosh discussion (#VD2M tier B, then #XQ8F) already delivered #87HB's *remote* half, but both those cards are implemented work awaiting verification, and #87HB still holds one genuinely undelivered ask: local persistence. Merging would bury that inside a QA record. I'll rewrite the card to say what's now true.

Retrying with `links` as one object — `related` lives inside it.

Now marking the options with their outcomes and trimming the settled open question.



Recording the outcome and the question on the thread.





**Compared — and kept separate on purpose.** The mosh discussion (#VD2M, then #XQ8F) already delivered this card's *remote* half: ssh panes are persistent by default via a hidden `tmux -L relay` holder on the host, re-attached after a Relay restart. But both those cards are implemented work in needs-verification, and merging #87HB into them would bury its one still-undelivered ask — **local** persistence (quit Relay, come back, the local build still running) — inside a QA record.

Changes to **#87HB**:
- Retitled: "The local half of the tmux gap: a shell that survives Relay quitting"; links now include #XQ8F and #VD2M.
- Comparison section rewritten to what's true today; options marked (2 and 4 rejected, 3 shipped remotely, 1 now much cheaper — reuse the #XQ8F holder locally instead of a bespoke daemon).
- Decision recorded in the thread; posted a question: is the local half still wanted? My recommendation is the local tmux holder if you ever miss it, or close the card as delivered-remotely if you don't. Card stays in discussing, waiting on you.

One footnote for later, not acted on: #VD2M's tier B (mosh+zellij persistent connect) was superseded by #XQ8F's holder — worth folding together when a verifier closes them.

<!-- relay:entry 20260925T051455Z-b7 author=owner kind=comment mode=plan -->
Plan this card.

<!-- relay:entry 20260925T051455Z-nz author=owner kind=event pane=switchboard -->
- ✦ owner moved this card · Discussing → Planning · a Plan turn started

<!-- relay:entry 20260925T051754Z-w4 author=agent kind=event model=glm-5.3 pane=switchboard turn=7e754d612668112cbf1a0c6e7b75c2e8/ca8605a84e674388b4f6cefba933da6f -->
- ✦ agent updated this card · replaced `## Done means`

<!-- relay:entry 20260925T051822Z-cz author=agent kind=event model=glm-5.3 pane=switchboard turn=7e754d612668112cbf1a0c6e7b75c2e8/ca8605a84e674388b4f6cefba933da6f -->
- ✦ agent updated this card · replaced `## Plan`

<!-- relay:entry 20260925T051833Z-jc author=agent kind=comment mode=plan model=glm-5.3 turn=7e754d612668112cbf1a0c6e7b75c2e8/ca8605a84e674388b4f6cefba933da6f -->
Planning turn — first the card's hash, then the code the plan must stand on: the #XQ8F holder, the wrapper, and where a local pane's shell is born.

The holder is host-agnostic — exactly what the plan needs. Now the local shell spawn path, the wrapper gate, SshConfig's session helpers, and the Options rows.

Still hunting the local shell spawn — it lives in `engine/`, not `src/`.

`engine/pty/Pty.h` takes an optional `program` (empty = `$SHELL`). Now who sets it, the option ids, and the restore-queue shape.

Reading the three regions that matter: the env export, the restore path, and the leaf save.

Big files — I'll pull exact lines with search_files instead.

One more probe: how the pane passes a program to the pty session.

The picture is complete: local panes never pass a program (`engine/pty/PtyUnix.cpp:89` defaults to `$SHELL`), so the wrap happens at session start. Re-reading the card for a fresh hash, then writing the two sections.

Writing `## Done means` first, then the plan.



**Planned.** The plan reuses #XQ8F's holder wholesale: a local pane's shell starts under the same `tmux -L relay` session named after its stable scrollback id, the restore queue offers the re-attach line exactly as remote panes do, and close-and-end plus the session list generalise with no new machinery — the code reads confirm every hook point (`startTerminal` in `src/PaneRuntime.cpp`, `serializeNode` in `src/RelayWindow.h`, `SshConfig.h`'s session helpers, the Options row at `RelayWindowSettings.cpp:363`).

One thing it needs from you before **Run**: confirm **on by default** (symmetric with the remote default you accepted; the Options row is the escape hatch) — it's flagged in the plan's Risks, and the Run agent will ask again in the handoff.

<!-- relay:entry 20260925T051833Z-wf author=owner kind=event pane=switchboard -->
- ✦ owner moved this card · Planning → Planned · the plan is on the card

<!-- relay:entry 20260925T052418Z-ys author=owner kind=comment mode=discuss -->
On by default

<!-- relay:entry 20260925T052431Z-p2 author=agent kind=decision model=glm-5.3 pane=switchboard turn=7e754d612668112cbf1a0c6e7b75c2e8/954422446f2b4148a361cd4284880e16 -->
2026-09-25, owner: "On by default." → `terminal/persistLocal` defaults on, symmetric with `ssh/persist`. The Risks question is settled; the Options row is the escape hatch. Card is ready to Run.

<!-- relay:entry 20260925T052450Z-gr author=agent kind=event model=glm-5.3 pane=switchboard turn=7e754d612668112cbf1a0c6e7b75c2e8/954422446f2b4148a361cd4284880e16 -->
- ✦ agent updated this card · waiting_on: owner → (unset); replaced `## Plan`

<!-- relay:entry 20260925T052459Z-pz author=agent kind=comment mode=discuss model=glm-5.3 turn=7e754d612668112cbf1a0c6e7b75c2e8/954422446f2b4148a361cd4284880e16 -->
The owner decided the flagged default. Recording it: a decision comment, the risk line updated, and `waiting_on` cleared.





Recorded on **#87HB**: decision quoted in the thread ("On by default"), the plan's Risks line now reads decided (`terminal/persistLocal` ships on, the Options row is the escape hatch), and `waiting_on` cleared.

The card is settled — press **Run** to hand it to a terminal pane.

<!-- relay:entry 20260925T052504Z-q5 author=owner kind=event pane=switchboard -->
- ✦ owner claimed this card · assignee agent, Planned → Executing, session b74e0c22

<!-- relay:entry 20260925T052504Z-q6 author=owner kind=progress pane=switchboard pane_token=b74e0c22-ed30-4b41-bf11-e19255da698a -->
Claimed (b74e0c22) · working on it from a terminal pane

Run pressed on iOS Safari.

<!-- relay:entry 20260925T060935Z-rn author=agent kind=progress model=glm-5.3 pane=b74e0c22 turn=0f2a4b37ddc044d8a72effebce96e6c6/cc6c13c416a14e2899c7aab47e67df75 -->
All six plan steps implemented and building; three notes where the code improved on the plan's wording:

1. Environment (plan step 2) — the session shell gets RELAY_PANE_ID/SESSION_TOKEN/RUNTIME_DIR/SHELL_EVENT on the *session command line* (`env`-style prefix), not through tmux.conf update-environment: one tmux server serves every pane, so the server's environment is whichever pane's client started it, and the command line is the only per-pane-reliable carrier. The conf itself needed no change.
2. Marks (plan Findings said "for free") — integration.bash had no $TMUX handling; the wrap now exists, gated on `RELAY_HOLDER=1` + `$TMUX` (the #S5SH DCS passthrough shape) so a user's own tmux keeps today's raw behaviour. 7772 row marks ride along in BEL form.
3. Restore runs the line automatically at the pane's first prompt — the exact shipped remote behaviour (`runRestoredRemoteLogin`), not a paused offer; the restored *queue* below it still waits for its keypress.

Also: panes under memory isolation keep their plain shell (a server that outlives the pane would defeat the unit), and a `local_login` line restores even when the option is now off — symmetric with remote_login lines, and the sessions list can kill what it leaves behind.

Build green (`scripts/relay-build`), `ctest -R sshconfig` green with new parser cases, `tests/test_ssh_shell.py` 45/45 green (needs `TMPDIR=/tmp`: the 15 WrapperTests failures under Relay's scratch TMPDIR are environmental, present without my changes too). Committing next.

<!-- relay:entry 20260925T061147Z-14 author=agent kind=event model=glm-5.3 pane=b74e0c22 turn=0f2a4b37ddc044d8a72effebce96e6c6/cc6c13c416a14e2899c7aab47e67df75 -->
- ✦ agent updated this card · links: {"commits": [], "evidence": [], "github": null, "plans": ["docs/SSH-AND-MOSH.md"… → {"commits": ["0fbf2cac", "7beab862"], "evidence": ["docs/qa_evidence/2026-09-25-…

<!-- relay:entry 20260925T061203Z-34 author=agent kind=event model=glm-5.3 pane=b74e0c22 turn=0f2a4b37ddc044d8a72effebce96e6c6/cc6c13c416a14e2899c7aab47e67df75 -->
- ✦ agent updated this card · appended to `## Tests`

<!-- relay:entry 20260925T061219Z-gq author=agent kind=event model=glm-5.3 pane=b74e0c22 turn=0f2a4b37ddc044d8a72effebce96e6c6/cc6c13c416a14e2899c7aab47e67df75 -->
- ✦ agent updated this card · appended to `## Execution Summary`

<!-- relay:entry 20260925T061300Z-8p author=agent kind=event model=glm-5.3 pane=b74e0c22 turn=0f2a4b37ddc044d8a72effebce96e6c6/cc6c13c416a14e2899c7aab47e67df75 -->
- ✦ agent updated this card · verify: (unset) → {"artifact": "system", "primary": "person", "also": ["script"], "human": "none",…

<!-- relay:entry 20260925T061307Z-kv author=agent kind=event model=glm-5.3 pane=b74e0c22 turn=0f2a4b37ddc044d8a72effebce96e6c6/cc6c13c416a14e2899c7aab47e67df75 -->
- ✦ agent moved this card · Executing → Needs verification · Implemented on main (0fbf2cac, 7beab862): holder start, mark passthrough, local_login restore, close-and-end, sessions list, option row; build + sshconfig + 45/45 test_ssh_shell green; the by-hand quit/relaunch pass is what waits in verification. · evidence docs/qa_evidence/2026-09-25-87hb-local-holder/ · implemented_by glm/glm-5.3

<!-- relay:entry 20260925T061352Z-xk author=agent kind=event model=glm-5.3 pane=b74e0c22 turn=0f2a4b37ddc044d8a72effebce96e6c6/cc6c13c416a14e2899c7aab47e67df75 -->
- ✦ agent updated this card · links: {"commits": ["0fbf2cac", "7beab862"], "evidence": ["docs/qa_evidence/2026-09-25-… → {"commits": ["0fbf2cac", "7beab862", "9f1032d9"], "evidence": ["docs/qa_evidence…
