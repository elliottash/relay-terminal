<!-- relay:entry 20260925T050020Z-p1 author=claude-code kind=note -->
### Claude Code · 2026-09-25 05:00
Fresh look (owner: "see if they're still needed"): **still needed**. Nothing of export/import or sync exists in `src/`, `backend/`, `scripts/` or `docs/`; the 2026-09-19 sweep (`0a676397`) deliberately left this card open and #XZZB's resolution names it as the only unfinished part of the Options work. Added `## Done means`, a detailed `## Plan` (settings locations re-verified today: QSettings `relay.conf` groups, `keybindings.json`, `relay/themes`, `~/.config/relay/switchboard` aliases, `local-models.json`, keyring and `remote/identity.py` as the never-export list) and a proposed `verify` block. Five owner questions are in the Plan's Risks: import merge vs replace, whether memories/local models are in scope, machine-specific options, sync design-only vs build (and its relation to #H0P3), and whether a CLI flag is wanted.
