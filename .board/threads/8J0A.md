<!-- relay:entry 20260926T022111Z-v1 author=agent kind=event model=gpt-6-astra pane=2e8d13e7 turn=dbc9af56def4427c91f4fecd2d277da4/4b4582c3fc9b489aa43a937e0ded95f7 -->
- ✦ agent created this card in Planned · .board/features/2026-09-25-c1-independent-parallel-development-verification.md

<!-- relay:entry 20260926T022427Z-jr author=agent kind=event mention=3MH4 model=gpt-6-astra pane=2e8d13e7 turn=dbc9af56def4427c91f4fecd2d277da4/4b4582c3fc9b489aa43a937e0ded95f7 -->
mentioned in #3MH4 · 2026-09-26 · agent

<!-- relay:entry 20260926T032356Z-c1 author=claude-code kind=progress model=claude-opus-5-5 -->
### Claude Code · 2026-09-26 03:23
Claimed (C1 verifier, subagent a9 under #3MH4, Claude family). Writing an independent acceptance suite against the landed modules and CLIs in temporary repos, then staging native/guest startup and an Xvfb capture. Findings go to #3MH4 with repros; no production module edits.

<!-- relay:entry 20260926T035251Z-c2 author=claude-code kind=evidence model=claude-opus-5-5 -->
### Claude Code · 2026-09-26 03:52
Acceptance run: `tests/test_parallel_landing_acceptance.py`, 30 passed on a clean export of main `c692bf22` (66 s). Fixed by B1 in `c459f5d6` after this suite hit them on `f097f19d`: stale ignored artifact masking a failing candidate, malformed marker read as legacy by land.py, pause returning before an in-flight publish. GUI: relay built at `4af516ea` (main does not compile since `dd898eae`, recorded on #HEY7), Xvfb, isolated XDG profile, private `TMUX_TMPDIR`; screenshots in `docs/qa_evidence/2026-09-26-verify-3MH4/`.

Open defects, each with its owner per the contract:
1. B3, high. Under the local tmux holder (no systemd user session, or isolation off), a second pane's shell starts in the first pane's worktree. `src/PaneRuntime.cpp:1116` re-exports `RELAY_SESSION_TOKEN`, `RELAY_RUNTIME_DIR`, … into each tmux-held shell but not `RELAY_START_DIR`, so `shell/integration.bash:36` `cd`s to the tmux server's global value, which is the first pane's. Measured: pane 887c62eb's worker cwd was `wt2abd46…` (its lease), while its shell (pid 786838) had `RELAY_START_DIR=…/wtf8cf7f…` and cwd `wtf8cf7f…` (pane 680a9d6a's). Screenshot 04. Repro: queue-mode project, isolated profile without a systemd user session, Ctrl+T.
2. B3, high. Pane leases are never released. Nothing in `src/` or `backend/worker.py` calls `workspace_context release`, so closing a pane or quitting Relay leaves its workspace `active`. `trees.LIVE_STATUSES` counts toward `max_workspaces`, so after 50 pane opens every development launch in that project is refused, and Live lists zombie active workspaces (screenshots 02/03).
3. B3, low. Live job age is always "0 s". `src/BoardPane.cpp:6976` renders `age_seconds` with `QJsonValue::toInt()`, which returns 0 for a non-whole double (real value 272.522). Use `qRound(toDouble())`.
4. B1, low. `IntegrationService.tree_status` sets `unlanded` to `tip != base_sha` and ignores the landing receipt, so a workspace whose job landed reads "active · work retained" in Live.
5. B1/B2, medium. Since `0eebe8f4`, one failed gate emits both `failed` and `author_required` handoffs, even with `[reconcile] enabled = false`. A live worker queues two author turns for one failure (measured through the real worker with a stub model: two `handoff_queued` for one job).
6. B1/B2, medium. Pane Board writes reach the queue only through `<repo>/scripts/land.py` (`agent.py _sync_board_writes`), and `land.py board_sync_via_queue` imports `relay_core` from `<repo>/backend`. A second project in queue mode has neither, so a pane's Board writes are never submitted; only a manual `relay-land board-submit` publishes them.
7. B1, low. `pause()`'s docstring says "submissions and workspaces continue", but `allocate_workspace`, `submit` and `submit_board_snapshot` all require `queue` mode. Only the raw landq `relay-land submit` records work while paused.
8. B2, low. `guest_launch` on a paused project exits 1 with a `WorkspacePreparationError` traceback instead of `{"ok": false, "error"}`: `main()` catches only LaunchError/ValueError/OSError. It still refuses, and the pane shows the last stderr line.
9. B2, doc. `docs/AGENT-SESSIONS-PROTOCOL.md` has two sections numbered `## 37.` (lines 9048 and 9386).

Outside #3MH4, seen while staging: an isolated XDG profile still shares `/tmp/tmux-$UID/relay` and adopted another session's detached pane; a `TMUX_TMPDIR` long enough to push the socket path past 107 bytes leaves the pane blank with no message; guest homes under an isolated profile link the real user's credentials.
