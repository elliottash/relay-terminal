<!-- relay:entry 20260925T141158Z-yw author=agent kind=event model=claude-fable-5-1 pane=be471986 turn=1fcbe747559b4edca3c622e83cb41fbe/4e0734b8d76242b3897fcbede8595652 -->
- ✦ agent created this card in Planned · .board/features/2026-09-25-python-console-pane-ipython-in-the-pty-on-relay.md

<!-- relay:entry 20260925T141527Z-mq author=agent kind=event model=claude-fable-5-1 pane=be471986 turn=1fcbe747559b4edca3c622e83cb41fbe/4e0734b8d76242b3897fcbede8595652 -->
- ✦ agent updated this card · replaced `## Plan`; tasks: 0/6 done

<!-- relay:entry 20260925T152647Z-c1 author=claude-code kind=progress -->
### Claude Code · 2026-09-25 15:26
Claimed (subagent of umbrella pane 69d471f7). Starting on the worker's console answer (workspace_plugins.py: start the kernel, return the argv for `jupyter console --existing <file>` or the `ipython` fallback), the OSC 133 startup file for both IPython and jupyter console, then the pane side ("New Python console" in the palette).

<!-- relay:entry 20260925T165805Z-y8 author=claude-code kind=progress -->
### Claude Code · 2026-09-25 16:58
Resumed after the previous builder was killed at the memory limit; its uncommitted worker work survived and is landing now: `workspace_activate {console: true}` answers `workspace_console` with the pty argv (`jupyter console --existing <file> --config=ipython_startup.py`, else `ipython`, else `python3`); `JupyterBackend.restart()` keeps the connection file so an attached console follows a restart; the startup file puts OSC 133 A/B/C/D marks on jupyter console and plain IPython (t:wz); `detect_repl` recognises venv entry-point scripts. Fixed on the way: `exit()` typed in an attached console stopped the shared kernel (ipykernel's `ask_exit` sets `exit_now`), so the agent's next `py_run_cell` came back `died`, intermittently. The kernel's `ask_exit` now only sends the keep-kernel payload (`py_kernel.KEEP_KERNEL_SOURCE`); a deterministic test covers it. Tests: `tests/test_python_console.py` (14, all run under a venv with ipykernel/jupyter_console), `tests/test_py_kernel.py`, `tests/test_workspace_plugins.py`, `tests/test_lang_router.py`: all OK.
