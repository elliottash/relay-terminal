<!-- relay:entry 20260925T050210Z-p1 author=claude-code kind=plan -->
### Claude Code · 2026-09-25 05:02
Fresh look (planning only; not claimed, status unchanged). Verdict: **partly done, still needed.**
Done elsewhere: Claude Code/Codex get the prompt box as their input line plus slash completion (GT7X, `d9075774`, protocol §26.8, `Pane::submitGuest`/`typeIntoGuest`, `relay_core/guest_slash.py`); canonical-mode prompts (`[Y/n]`, `read -p`) already receive lines (`relay::input::targetFor`, `Pane::sendLineToProgram`, `952fe042`); Terminal mode + a clear request runs nothing (#94V5); a Python/Stata `program` classifier exists worker-side (#33G0, `lang_router.py`).
Remaining: raw-mode REPLs (python3, psql, sqlite3, node) still queue behind the program; no Program mode/chip; GUI never sends `foreground_program` to the router nor handles `route == "program"`; no per-program completion; a Terminal-mode typo still starts the agent fix loop (`src/PaneRuntime.cpp:1224` → `startFix`).
Wrote `## Done means`, `## Plan` (8 steps, 6 owner questions incl. ownership split with #33G0), and proposed `verify` (script + ai-visual, human required).

<!-- relay:entry 20260925T141405Z-30 author=agent kind=event model=claude-fable-5-1 pane=be471986 turn=1fcbe747559b4edca3c622e83cb41fbe/4e0734b8d76242b3897fcbede8595652 -->
- ✦ agent updated this card · labels: ["feature"] → ["feature", "panes", "agent-ui"]; appended to `## Decisions`; tasks: 0/8 done

<!-- relay:entry 20260925T141806Z-a8 author=s976-program kind=progress -->
### s976-program · 2026-09-25 14:18
Claimed #S976; implementing the settled eight-step plan in the shared checkout.

<!-- relay:entry 20260925T150041Z-sv author=agent kind=event model=glm-5.3 pane=69d471f7 turn=1fcbe747559b4edca3c622e83cb41fbe/52cccb83d761491fba0f3a860633c7fd -->
- ✦ agent updated this card · tasks: 3/8 done

<!-- relay:entry 20260925T150054Z-gf author=agent kind=progress model=glm-5.3 pane=69d471f7 turn=1fcbe747559b4edca3c622e83cb41fbe/52cccb83d761491fba0f3a860633c7fd -->
### Umbrella session · 2026-09-25 (state refresh, pausing)

The builder session (s976-program) ended after landing four commits on `main`: `0ccf9604` (step 1 policy + input tests), `c45d3fab` (step 6 ProgramCompletion library + tests), `4646a7e6` (GUI link), `48e86790` (step 7 typo refusals). Steps 2–5 are **written but uncommitted** in the working tree (`src/Pane.h` +80, `src/PaneRuntime.cpp` +38 with `foreground_program` and `route == "program"`, `src/RelayWindow.h` +33, `src/ShellHighlighter.h`, `src/Keymap.h`, `tests/keymap_test.cpp` +49 with the `PROGRAM · %1` chip). `ctest -R '^(keymap|input|programcompletion)'` passes 3/3 on that tree.

Next session resumes from the tree: land steps 2–5 through `scripts/land.py` (excluding any hunk belonging to the held #E85D/#F8R7 cluster in the same files), then step 4's busy-action, then docs + live evidence (`db`). Card stays executing.
