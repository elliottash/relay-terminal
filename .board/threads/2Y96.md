<!-- relay:entry 20260919T200730Z-ge author=owner kind=comment mode=plan -->
Plan this card.

<!-- relay:entry 20260919T201519Z-b6 author=agent kind=event model=glm-5.3 pane=switchboard turn=6a1d800a64934b339265f107916a45be/8a6caf562b504d26a9bdec2d455bc3d9 -->
- ✦ agent updated this card · replaced `## Plan`

<!-- relay:entry 20260919T201534Z-7s author=agent kind=comment mode=plan model=glm-5.3 turn=6a1d800a64934b339265f107916a45be/8a6caf562b504d26a9bdec2d455bc3d9 -->
`RelayWindow.h` is too large for a single read; I'll pull the relevant regions out with multi-line searches.











































I have the full picture now — pane creation, the attach model, session buckets and the index all checked against the code. Writing the plan onto the card.



**Done:** the plan is on #2Y96, replacing the stage-6 sketch with six checked steps — 6a (panes take the opening cwd, with every launch-workspace fallback in `paneNode`/`createPane`/`newWindowAt`/`activeCwd` given a real answer), explicit `session_dir` buckets (`<keyFor(project)>` vs one shared `loose`, both halves in one commit), 6b via a pending workspace that only lands at the next `configure`, a new `session_refile` worker message for the agreed re-file-on-attach, the `agent-write` attach trigger plus the once-per-pair hint, and plain `relay` opening a tab in the current directory after a restore.

It needs one decision from the owner, in Risks: pre-change saved layouts reopen old panes with the launch workspace recorded in them — I recommend leaving them as they are rather than silently remapping on restore.

<!-- relay:entry 20260919T232746Z-zc author=owner kind=event pane=switchboard -->
- ✦ owner moved this card · Ready to start → In progress · changed in the Switchboard

<!-- relay:entry 20260919T232746Z-zd author=owner kind=event pane=switchboard -->
- ✦ owner moved this card · In progress → Ready to start · changed in the Switchboard

<!-- relay:entry 20260919T232807Z-pa author=owner kind=comment mode=discuss -->
compare this to other issues / designs we have discussed on this, to check its fresh

<!-- relay:entry 20260919T233106Z-c3 author=agent kind=event model=deepseek/deepseek-v4.1-flash pane=switchboard turn=b73503d78cff4dafb8c2b8c2c2cb53c1/6d0310fa06c1496aa48a3704dbf844dd -->
- ✦ agent updated this card · appended to `## Freshness check against the rest of the board (2026-09-19)`

<!-- relay:entry 20260919T233125Z-f7 author=agent kind=note model=deepseek/deepseek-v4.1-flash pane=switchboard turn=b73503d78cff4dafb8c2b8c2c2cb53c1/6d0310fa06c1496aa48a3704dbf844dd -->
Compared #2Y96 against every card and design doc that touches the workspace, the project model or\nsessions: **it is fresh — no card or doc proposes a per-pane agent workspace, and nothing needs\nmerging.** The design docs only ever discuss the *board's* project (`SWITCHBOARD-DESIGN.md` §4.1,\n`PROJECT-INIT-AND-IMPORT.md`), and #JN7X put the attach model and the pane's own workspace out of\nscope on purpose.\n\nWhat the comparison did turn up is four edges, now written onto the card as\n\"Freshness check against the rest of the board\":\n\n1. **#64KE's known gap 6 is what step 6 removes** — that card documents \"`--workspace` implies\n   `--fresh`; there is no way to say 'restore *and* also open this directory'\". Recommendation:\n   `--workspace` keeps implying `--fresh`, only plain `relay` gains the tab, and #64KE's gap is\n   corrected when this lands.\n2. **`loose` needs one sentence** — #916B's \"loose cards\" are board filing (and its inbox is gone);\n   this card's `loose` is a session bucket. Also its protocol 14.3 already *filters* the Sessions\n   list by project, so \"the conversation list does not scatter\" here should say rows are filed\n   under the right workspace, not that the filter gets built.\n3. **#TVE1 reads the same key** — the bucket helper in step 2 is the \"one place both read the key\n   from\" that card asks for.\n4. **The docs already contradict each other.** `ARCHITECTURE.md:1885` and the protocol's cost guard\n   (§11, \"Too wide to crawl\") state the post-6a world as fact, while `ARCHITECTURE.md:1448` and\n   `SWITCHBOARD-DESIGN.md:152` still give \"frozen at creation, inherited from the launch directory\"\n   as the reason the board ignores `workspace()`. This card should carry that doc pass.\n\nSmaller neighbours named on the card: #0STR's header tooltip is the visible surface of this bug;\n#G8DK's local-alias scope follows the workspace and changes with 6a; #E99H and #3KB7 state the\nconfinement as \"one directory\" and will want a re-read (the ssh path, #S5SH, already confines to\n\"the directory their shell is in\", so this makes the two agree).

<!-- relay:entry 20260919T233141Z-bf author=agent kind=comment mode=discuss model=deepseek/deepseek-v4.1-flash turn=b73503d78cff4dafb8c2b8c2c2cb53c1/6d0310fa06c1496aa48a3704dbf844dd -->
**Done:** compared #2Y96 against the board and the design docs — **it's fresh; nothing to merge.** No card or design doc proposes a per-pane agent workspace (`SWITCHBOARD-DESIGN.md` §4.1 and `PROJECT-INIT-AND-IMPORT.md` only ever discuss the *board's* project, and #JN7X deliberately left the attach model and the pane's own workspace out of scope).

I appended a `## Freshness check against the rest of the board (2026-09-19)` section and a thread note. The four edges worth folding in:

1. **#64KE known gap 6 is what step 6 removes** — it documents "`--workspace` implies `--fresh` … no way to say 'restore *and* also open this directory'". My recommendation: leave `--workspace` as it is, only plain `relay` gains the tab, and correct #64KE when this lands. That's the one decision I'd like from you.
2. **`loose` needs a sentence** — #916B's "loose cards" are board filing (its inbox is gone); this card's `loose` is a session bucket. And its protocol 14.3 already filters the Sessions list, so "the conversation list does not scatter" should mean *rows filed under the right workspace*, not "build the filter".
3. **#TVE1 reads the same key** — the bucket helper in step 2 is the "one place both read the key from" that card asks for; the acceptance line should split between the two cards.
4. **The docs already contradict each other** — `ARCHITECTURE.md:1885` and the protocol's cost guard (§11) state the post-6a world as fact, while `ARCHITECTURE.md:1448` and `SWITCHBOARD-DESIGN.md:152` still give "frozen at creation, inherited from the launch directory" as the reason the board ignores `workspace()`. This card should carry that doc pass.

Smaller ones also named: #0STR's header tooltip is the visible surface of this bug; #G8DK's local-alias scope follows the workspace and changes with 6a; #E99H and #3KB7 say the confinement is "one directory" and will want a re-read (the ssh path #S5SH already confines to "the directory their shell is in", so this makes local and remote agree).

<!-- relay:entry 20260926T040615Z-jh author=codex kind=note -->
### Codex · 2026-09-26 04:06 UTC
Added a 2026-09-26 plan refresh: the workspace inheritance and restore defect persists. Targeted checks replace full-suite instructions; preserving recorded old-layout sandboxes is the recommended unresolved owner choice.
