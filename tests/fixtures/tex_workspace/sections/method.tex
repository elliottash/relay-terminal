\section{Method}\label{sec:method}

The method section is pulled in with \verb|\include|, which starts a new
page and writes its own auxiliary file next to the main one.

It also cites Lamport~\cite{lamport1994} so the bibliography has two entries.
