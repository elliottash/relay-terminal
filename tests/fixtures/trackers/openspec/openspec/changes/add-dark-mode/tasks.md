# Tasks

## 1. Resolve the tokens once
- [x] 1.1 Extract the palette into `:root` custom properties
- [ ] 1.2 Pin the published values in a unit test

## 2. Render it
- [ ] 2.1 Add the toggle to the header
- [~] 2.2 A tilde box is NOT in progress in OpenSpec; it reads as unfinished
- [] 2.3 An empty box is unfinished too
