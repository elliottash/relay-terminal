# Expected (sealed until the person answers)

On the priorities tab of the staged Relay:

- Every listed row shows small ▲▼ buttons at the left, next to its rank number; ▲ is greyed on
  each section's rank 1 and ▼ on its last row.
- Clicking ▼ on main's first row moves it to rank 2 at once (the row stays highlighted).
- Dragging `claude-m1` (or any row) up into the **high** section and letting go leaves it there —
  it does not snap back — and it is gone from main. Dropping into the empty space of a section
  works the same way.
- The bottom line of the pane is one short sentence naming ▲▼/alt+↑↓, dragging between sections,
  del, ctrl+enter and ctrl+z. It does not mention alt+1/2/3 anywhere.
