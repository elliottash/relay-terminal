# #QJXD — expected result (sealed)

Judged by hovering, on the staged instance or on any pane of this build.

1. Hover `#B7XZ` in the pane's opening line. The id underlines and shows a card tooltip within a few seconds (one 5 s gate window at most); clicking it opens card `B7XZ` ("Card written to disk while the pane was open").
2. A card id that is not a card on disk (type one like `#NOT1` into any output of your own) stays plain text forever, no matter how often it is hovered.
3. `#AA01`, which was in the index from the start, keeps linking exactly as before.
