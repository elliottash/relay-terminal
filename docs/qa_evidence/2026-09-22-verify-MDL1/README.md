# MDL1 independent verification — 2026-09-22

**Verdict: 17 pass, 2 partial/blocked; not a full QA pass.** All 19 items were reviewed against the final decisions. One live defect was fixed in `85d0dd0f7dea0023dceaea9b6b8d4d858156805c`: the window consumed Alt+Up/Down before the embedded priorities editor could reorder. The exact commit tree builds. The card remains `needs-verification` on main; another session's uncommitted front matter (`executing`, `codex`, links) was deliberately preserved.

## Environment and limits

Fresh Xvfb displays, isolated HOME/XDG_CONFIG_HOME/XDG_DATA_HOME/runtime directories, `RELAY_KEYRING=off`, fake provider keys, no model-generation network requests. The actual GUI and worker were used except the explicitly labelled startup protocol fixture. The Codex effort run uses a fake catalog-reporting CLI. These are UI/configuration checks, not provider authentication or inference checks. No delegation; no relay_board tools were available.

The shared binary was built using `scripts/relay-build --target relay` (initial build 2026-09-21.21H.01, fix build .04). Other sessions' code was present in that build; only the shortcut fix was also compiled from its exact proposed commit tree by land.py. Original evidence drives were read and adapted into the subdirectories here. Screenshots are newly captured, not copied from implementation evidence. The `extra` run was driven interactively after its startup script: screenshot names record each step below. The scripts retain their `/tmp/mdl1verify-artifacts` output locations for repeat runs. Original historical comments in copied drivers are not verdicts: the table below is the verification record.

## All 19 checks

| # | Result | Final expectation and fresh evidence |
|---|---|---|
| 1 | **Partial** | New API pane uses rank 1 and its level: [defaults/01-new-pane-rank1.png](defaults/01-new-pane-rank1.png) shows glm-5.3-flash/low despite a different saved provider. Deferred Codex displays gpt-6-astra/xhigh ([effort/i-codex-pane.png](effort/i-codex-pane.png)). `startup/run.txt` and `first-prompt-requests.jsonl` prove no configure before the prompt and exactly one ask after configuration in a live GUI with a **fake worker**. An authenticated real guest process/turn was not exercised; that part stays unchecked. |
| 2 | **Pass** | Per-pane pick does not change the default: [defaults/03-pane-b-still-rank1.png](defaults/03-pane-b-still-rank1.png), left kimi-k3, right new pane glm-5.3-flash/low. |
| 3 | **Pass after fix** | Ctrl+Shift+M → priorities → main rank 2 → Alt+Up. [extra/reorder-retry.png](extra/reorder-retry.png) shows the failure before the fix; [fixed-reordered.png](extra/fixed-reordered.png) shows kimi-k3 promoted, and [fixed-new-pane.png](extra/fixed-new-pane.png) shows the next pane on kimi-k3. Fix is confined to the window's shortcut dispatch while inside the sectioned picker. |
| 4 | **Pass** | From glm-5.3-flash (outside the two-row main list), `/swap` goes to main rank 1 kimi-k3; [extra/swap-retry-later.png](extra/swap-retry-later.png) still shows the swap explanation after the worker response; [swap-back.png](extra/swap-back.png) returns the chip to glm-5.3-flash. The first replay's hint covered the sentence, so that replay alone was not accepted as proof. |
| 5 | **Pass** | Corrected final box design: class headers are not selectable; arrows select models; Right/Left expands/collapses the current class, not the pane mode. [box/b-altm-classes.png](box/b-altm-classes.png), [c2-down-again.png](box/c2-down-again.png), [d-right-expands-main.png](box/d-right-expands-main.png), [e-left-collapses-main.png](box/e-left-collapses-main.png), [f-filter-across-classes.png](box/f-filter-across-classes.png), [g2-picked-flash.png](box/g2-picked-flash.png). Enter on flash changes to glm-5.3-flash; chip contains only its name. Escape from filtering leaves the original model. |
| 6 | **Pass** | [effort/b-alt-e-kimi-three.png](effort/b-alt-e-kimi-three.png), [c2-alt-e-openai-xhigh.png](effort/c2-alt-e-openai-xhigh.png), [j-alt-e-codex-six.png](effort/j-alt-e-codex-six.png): current level highlighted, complete list inside the window, no scrollbar. |
| 7 | **Pass** | [extra/board-model-box.png](extra/board-model-box.png) and [terminal-model-box.png](extra/terminal-model-box.png): same class/model rows and current highlight, no protocol role suffix. The test board is only under the isolated temporary project. |
| 8 | **Pass** | [effort/i-codex-pane.png](effort/i-codex-pane.png) says gpt-6-astra; [extra/claude-name.png](extra/claude-name.png) says claude-opus-5 after explicitly selecting the guest route. This tests names, not a successful guest inference turn. |
| 9 | **Pass** | Corrected door: Ctrl+Shift+M, priorities. Type sonnet, Ctrl+Enter adds; Delete removes; Ctrl+Z restores ([extra/add-before.png](extra/add-before.png), [add-after.png](extra/add-after.png), [delete-after.png](extra/delete-after.png), [undo-after.png](extra/undo-after.png)). Available folds two GLM providers into one model row with both routes in via ([extra/folded.png](extra/folded.png)). |
| 10 | **Pass** | No-argument restart preserves four terminal panes' preset/model/role/effort, including a flash pane: [extra/modes-before-quit.png](extra/modes-before-quit.png), [modes-after-restart.png](extra/modes-after-restart.png), `modes-saved.json`, `modes-resaved.json`, `restore-check.txt`. The flash pane stores its own main model k3 and restores its flash role; its displayed model is glm-5.3-flash. |
| 11 | **Partial / blocked** | Six `test_web_model_name.py` cases pass, including the phone's JS naming function compared with the worker (`web-name-tests.txt`). No paired phone/browser session was available or driven. This is not a live phone UI pass. |
| 12 | **Pass** | [pane/b-closed.png](pane/b-closed.png), [c-reopened-priorities.png](pane/c-reopened-priorities.png), [h-escape-focus-back.png](pane/h-escape-focus-back.png); [extra/retired-key.png](extra/retired-key.png) shows Ctrl+Alt+M opens nothing. Retargeting proved by changing the right terminal through Models, reopening it from the left, then changing only the left: [extra/retarget-right-changed.png](extra/retarget-right-changed.png), [retarget-left-changed.png](extra/retarget-left-changed.png). |
| 13 | **Pass** | Alt+1..4; provider rules ([review/a-providers-dividers.png](review/a-providers-dividers.png)), no advanced-provider search result ([d-options-no-advanced-row.png](review/d-options-no-advanced-row.png)), alphabetical provider groups/no recent ([e-available-alphabetical-no-recent.png](review/e-available-alphabetical-no-recent.png)), gemini-flash-lite unticked ([f-gemini-flash-lite-unticked.png](review/f-gemini-flash-lite-unticked.png)), sectioned priorities/no lite ([g-priorities-sections.png](review/g-priorities-sections.png)). Jobs reads actual worker resolutions and updates after a summary override ([jobs/a-jobs-runs-on.png](jobs/a-jobs-runs-on.png), [d-runs-on-followed.png](jobs/d-runs-on-followed.png), [e-override-cleared.png](jobs/e-override-cleared.png)). |
| 14 | **Pass** | Corrected scope: an unlisted terminal model can be unticked; an entry in a terminal priority list stays pinned, and a lite-only list does not pin it. [extra/tick-off.png](extra/tick-off.png) → [unavailable-filter.png](extra/unavailable-filter.png) shows no match in Alt+M. `review/lists-after.txt` shows the lite list unchanged. Explicit `/model name` is allowed a fallback to all usable entries; it is not a prohibition on explicit selection. Catalog/picker tests cover the shared availability rule used by the first lookup. |
| 15 | **Pass** | [extra/tick-before.png](extra/tick-before.png), [tick-double.png](extra/tick-double.png): a double click on Gemini's tick does not switch the served terminal off glm-5.3. The double toggles leave the checkbox checked; a single subsequent click unticks it. |
| 16 | **Pass (catalog fixture)** | [effort/j-alt-e-codex-six.png](effort/j-alt-e-codex-six.png) has xhigh and ultra from a fake Codex catalog, read by the real worker; [f2-relay-free-tooltip.png](effort/f2-relay-free-tooltip.png) and [g1-alt-e-says-why.png](effort/g1-alt-e-says-why.png) show fixed-level behavior for Relay Free. No provider turn was made. |
| 17 | **Pass** | [pane/k-before-quit.png](pane/k-before-quit.png), [l-after-restart.png](pane/l-after-restart.png), `saved-layout-check.txt`: Models survives beside the terminal on available after a no-argument restart. Layout checker finds exactly one models node beside a terminal. |
| 18 | **Pass** | [fresh/no-keys-first-run.png](fresh/no-keys-first-run.png): fresh config, no provider keys, terminal left, Models right on providers, provider rows expanded. This item checks onboarding layout, not authenticated harness availability. |
| 19 | **Pass** | Corrected documented sequence: edit ranking → restart worker → fill from defaults. Only `/tmp/mv-ranking-data/backend/relay_core/model-ranking.md` was changed (kimi score 44→99); the repository file was untouched. Real worker/GUI, [ranking/after-fill.png](ranking/after-fill.png) and `conf-after.txt`: kimi is first in high and main, GLM second. `ranking/ranking-edited.md` preserves the fixture. |

## Validation and remaining blockers

- `ctest.txt`: nine targeted C++ test executables passed. `fix-tests.txt`: modelpicker and modelspane passed again after the shortcut fix.
- `ranking-tests.txt`: 40 passed. `web-name-tests.txt`: 6 passed. `startup/run.txt`: first-prompt protocol fixture passed.
- `build-fix.txt`, `land-fix.txt`: wrapper build and exact-tree landing build passed. Fix commit: `85d0dd0f`.
- `tests-check.json`: read-only `TestsCommands.check_card('MDL1')` returns no findings/blocks, but its ids are empty and revision points to the old first linked commit; it is not a substitute for the explicit run logs here. It also reports an unrelated expired run-level signal from another session.
- `board-check-MDL1.json`: pre-existing format issues: MDL1 contains non-Crockford L, a10–a16 are three-character task markers, and the old thread is unsorted. No rename, historical rewrite or global `--fix` was performed.
- Closing is blocked on real authenticated guest startup and live phone presentation. The contested front matter and old links were not absorbed. Commit/evidence references are recorded in the verdict and append-only thread instead. CFG1, ACT1, RLP7 and XJSN were not edited.
