# /update (card #HDA9) — how this evidence was taken, 2026-09-19 ~00:00–00:30

Four agent sessions were working in this tree while the evidence was gathered, so the
build result has a story attached; run.log alone would misread without it.

Timeline (all times the same night):

- ~00:05  Full clean build of the `relay` target WITH the complete /update wiring
  (scripts/relay-update.py, tests/test_update.py, and the RelayWindow.h + Pane.h +
  Keymap.h + CMakeLists.txt + README.md changes): `[100%] Built target relay`.
  That is the state the /update code was verified in.
- 00:20:54  A concurrent session (ProjectInit / protocol 19.12 work) rewrote src/Pane.h
  from a buffer that predated /update, dropping its three hunks: the `onUpdateApp`
  member, the `/update` branch of the slash-command handler, and the help-card row.
  RelayWindow.h still assigned `pane->onUpdateApp`, so the tree stopped building for
  everyone (`error: 'class Pane' has no member named 'onUpdateApp'`).
- ~00:24  The three hunks were restored into src/Pane.h byte-identically (anchored
  insertions, each required to match exactly once). The `onUpdateApp` and
  `prompthistory` errors cleared; the only remaining build errors are the concurrent
  session's own `m_initSnoozed` member, not yet declared in RelayWindow.h — left to
  that session on purpose.

So in run.log: the unit tests pass, `check` refuses a source checkout as designed, and
the live dry-run downloads the right asset for this machine (ubuntu24.04 arm64, the
beta.2 release current that night) and verifies it against the release's SHA256SUMS.
The build step fails only on the unrelated in-flight ProjectInit feature; the same
/updating code had built clean an hour earlier in the same tree (top of this note).

What was deliberately NOT exercised here: installing over this machine's Relay (it is
a source checkout, and the guard refuses — that refusal is itself tested), the pkexec
password dialog, and the restart-into-the-new-binary path. Those need a .deb-installed
Relay; on one, `/update` in any prompt box is the whole test.
