# Compass Actions implementer evidence

Run `drive.sh` after `scripts/relay-build --target relay`. It starts an isolated Relay under Xvfb,
opens Actions with Ctrl+?, and captures the Compass and its Remote dropdown.

- `01-compass.png`: search centred among the three fixed group arms, with results below.
- `02-remote-menu.png`: Right twice, then Down opens Remote from the search box.
- `03-narrow.png`: the same palette after resizing the window to 520 × 700.

The independent verification of the earlier shortcut and Sessions cards remains paused by the owner.
