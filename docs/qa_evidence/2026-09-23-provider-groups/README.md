# Models provider groups (#P3KD)

Built Relay and drove it under Xvfb with a fresh, isolated home and fake provider keys (`drive.sh`).

- `01-providers.png`: Profiles above the groups, with Guest Agents expanded.
- `01b-provider-keys.png`: lower on the same page, Subscription Keys and Pay-as-you-go Keys with their provider rows.
- `02-options-defaults.png`: the Model priority lists fill buttons found by searching Options; the Providers tab behind it has no Defaults group.

The driver uses no real account keys and makes no model request.
