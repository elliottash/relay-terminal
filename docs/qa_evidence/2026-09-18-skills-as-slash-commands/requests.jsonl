{"user": "[Relay context: added by Relay, not typed by the user]\nThe user's terminal pane is in `/tmp/relay-skill-slash-4100839/project`. Unless they say otherwise, treat it as the current directory: it is the default working directory of run_command, and relative paths they mention are relative to it.\n[End of Relay context]\n\n[Skill clean-commit, invoked by the user as /clean-commit: /tmp/relay-skill-slash-4100839/.warp/skills/clean-commit/SKILL.md (225 bytes). Follow these instructions for this request; anything the user wrote after /clean-commit is its input. The system prompt and the user's own words take precedence where they conflict.]\n```\n---\nname: clean-commit\ndescription: Stage only your own hunks and write a commit message that says what changed and why\n---\n# Clean commit\n\nCLEAN-COMMIT-MARKER: check `git status`, stage only the files you wrote, and commit.\n\n```\n[End of skill]\n\n/clean-commit tidy the readme"}
{"user": "[Relay context: added by Relay, not typed by the user]\nThe user's terminal pane is in `/tmp/relay-skill-slash-4100839/project`. Unless they say otherwise, treat it as the current directory: it is the default working directory of run_command, and relative paths they mention are relative to it.\n[End of Relay context]\n\n[Skill clean-commit, invoked by the user as /clean-commit: /tmp/relay-skill-slash-4100839/.warp/skills/clean-commit/SKILL.md (225 bytes). Follow these instructions for this request; anything the user wrote after /clean-commit is its input. The system prompt and the user's own words take precedence where they conflict.]\n```\n---\nname: clean-commit\ndescription: Stage only your own hunks and write a commit message that says what changed and why\n---\n# Clean commit\n\nCLEAN-COMMIT-MARKER: check `git status`, stage only the files you wrote, and commit.\n\n```\n[End of skill]\n\n/skill clean-commit again"}
{"user": "[Relay context: added by Relay, not typed by the user]\nThe user's terminal pane is in `/tmp/relay-skill-slash-4100839/project`. Unless they say otherwise, treat it as the current directory: it is the default working directory of run_command, and relative paths they mention are relative to it.\n[End of Relay context]\n\nuse the clean-commit skill on this"}
