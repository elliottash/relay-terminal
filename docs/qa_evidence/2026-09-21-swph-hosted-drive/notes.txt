PASS a fresh profile starts with remote control off (True, no [remote] section: True); the plug menu's first item opens the pairing dialog (True), which turns the service on by itself: the sidecar runs (True), the desktop registers at join.relay-terminal.ai (health desktops 0 → 1), relay.conf remembers alwaysOn=true (True) with address=relay-terminal.ai (True), the dialog shows a code shaped 'ABCD 4829' (True) and the link is hosted (https://join.relay-terminal.ai/pair#…, 135 chars) (shots step14a-e)
PASS the welcome screen at https://join.relay-terminal.ai/ leads with 'Enter the code from your desktop'; the eight characters from the dialog pair this phone: the same five digits show on both sides ('25969' — shot step15c is the desktop's), Allow typing lands it in the inbox (True, one screen drawn: True) as 'full', link 'connected'; the rendezvous's /v1/health and the desktop's audit carry neither the PIN nor the pairing secret (True), and the PIN is in no console line (True). The desktop's audit has code_used for CEKJ exactly once with no failed try (True), and the dialog's code row ended: it offers "New code" (True; shot step15e reads "Used") (shots step15a-e)
NOTE the inbox led with the notifications offer on arrival: True (step 18 taps it)
NOTE the third wrong PIN was answered with the code-has-stopped-working sentence (the served meet.js has the fix)
PASS three wrong PINs on a fresh code (QXEK) burn it: the page counts them (['That PIN is not right. Wrong PINs count: after three, this code stops working. Check the four digits on your desktop, then try again.', 'That PIN is not right. Wrong PINs count: after three, this code stops working. Check the four digits on your desktop, then try again.', 'This code has stopped working — it has been used, or too many wrong PINs were tried. Choose “New code” on your desktop.']), and the *right* PIN afterwards is refused — 'There is no pairing code with those letters. Codes last ten minutes — if the one on your desktop is older than that, choose “New code”.' — with the phone still on the welcome screen (True). The desktop's audit has code_burned with failures=3 (True) and the dialog's row reads "Closed" with a "New code" button (shot step16b). That button (pressed) mints MAXN, which pairs a second device (True) (shots step16a-e; the old code was CEKJ)
PASS a pairing link opened in Safari on an iPhone that is not installed shows the install card (True) and hands the link over instead of pairing: screens ['screen-welcome'], the copy row is up (True) with the link itself (True), and ten seconds later nothing has paired (True) — the desktop was never asked: its audit has 2 pairing(s) before the tab and 2 after, and shot step17b is the dialog with no approval row. The same link in an installed app pairs (True, left at 'https://join.relay-terminal.ai/'; the audit now has 3), which is the proof the tab never spent it (shots step17a-c). Lead: 'Add Relay to your Home Screen first, then open it from there and\n          enter'
PASS the first arrival after pairing leads with one button: {'top': True, 'offer': True, 'text': 'Turn on notifications', 'first': True, 'permission': 'default'}; one tap turns both switches on ([['notify-switch-finished', True], ['notify-switch-needs', True]]) and the offer collapses (True), leaving the settings row ('Stop notifying this phone') where it is changed afterwards (shots step18a, step18b). The subscription itself is the browser's one stub (pushManager.subscribe); everything on the desktop's side of it is real.
PASS a second device typed the same kind of code ('80069' on both sides) and was let in with Allow viewing: it reads 'view' (shots sw00a, sw00b)
PASS the plug menu's switch turns remote control off: the rendezvous drops the desktop (desktops 0), the phone says 'offline', alwaysOn=false is written (True). Turned on again from the same menu the desktop is back (desktops 1, alwaysOn=true: True) and the phone reconnects (on its own) straight into the inbox with 1 pane(s) and no pairing (True, screens ['screen-inbox']) (shots step19a-e)
PASS every code the dialog showed stayed the code: 5 minted (['CEKJ', 'QXEK', 'MAXN', 'AYHZ', 'GVGA']), none ended by the service's move to join.relay-terminal.ai (code_expired: [])
PASS the inbox of the phone paired by typed code ('full') leads with the Switchboard row, above its 1 pane(s) (True): it says '1 waiting on you' and 'harbour · 9 open cards'; on disk ['W8TQ'] wait on the owner. The app badge was last set to 1 (app.js counts the panes that need you plus the cards that do). The welcome's features: ['panes', 'agent', 'compose', 'voice', 'screen', 'takeover', 'history', 'pane_state', 'board'] (shot sw01a)
PASS the second device, paired with Allow viewing ('view'), sees the 1 pane(s) and no Switchboard row (True): the features of its 4 welcome(s) — it has reconnected through step 19 — are ['panes', 'agent', 'compose', 'voice', 'screen', 'takeover', 'history', 'pane_state'] (no "board"), and it has been sent 0 board_event(s) (shot sw01c)
PASS the row opens the board ('harbour', laid out for a phone): tabs ['All', 'features', 'bugs', 'design', 'marketing', 'planning', 'deferred', 'done'] (board.yaml's: ['features', 'bugs', 'design', 'marketing', 'planning', 'deferred', 'done']); "Waiting on you" is pinned first with 1 card (['waiting-on-you', 'W8TQ']); then the stages in the desktop's order with its counts — the phone [('inbox', 2), ('discussing', 2), ('planning', 2), ('planned', 1), ('executing', 1), ('needs-verification', 1), ('needs-qa', 0), ('verified', 0), ('done', 1)], the files by the desktop's own column rules [('inbox', 2), ('discussing', 2), ('planning', 2), ('planned', 1), ('executing', 1), ('needs-verification', 1), ('needs-qa', 0), ('verified', 0), ('done', 1)] (shots sw02a, sw02b; Done starts folded, as on the desktop)
PASS the desktop's own Switchboard pane, opened beside the terminal with Ctrl+Shift+S and read off the window by OCR, counts {'inbox': 2, 'discussing': 2, 'planning': 2, 'planned': 1, 'executing': 1, 'needs-verification': 1, 'needs-qa': 0, 'verified': 0, 'done': 1} (shot sw02b)
PASS #W8TQ opens as 'Tide table for the harbour page' ['Discussing', 'waiting on you', '✦ agent', 'features', 'feature']: the body by section ['Issue', 'Thread'], no comment marker in the text (True), the thread [('owner', 'comment'), ('agent', 'question')] with the question set apart (its left edge '3px rgb(229, 192, 123)' against a comment's '1px rgb(58, 40, 34)'), its three options as buttons (['1Above the map — the first t', '2In the sidebar — out of the', '3On a page of their own — ro']) and the send button reading 'Answer' (shot sw03a)
PASS tapping option 1 prefills the reply ('1. '); Answer puts a decision in the thread on the phone (True) and in .switchboard/threads/W8TQ.md (True): author='owner' kind='decision' text='owner, from Linux Chrome: “1. Above the map, and keep it to three lines.”' — the owner's words, quoted (True). The desktop's status line, read off the window: 'Comment on #W8TQ from Linux Chrome | ~ |' (shots sw03b-d)
PASS on a card with no question the button reads 'Comment'; the comment is in threads/N2BX.md once (1) as kind='note' author='owner' (shot sw04a)
PASS Move… with a reason: the file's front matter went 'planning' → 'planned', the reason is in the thread (['- ✦ owner moved this card · Planning → Planned · agreed on the quay this morning (from Linux Chrome)']), the phone said 'Moved to Planned.' and its list has #D4MV under ['planned'] without a reload; the desktop's status line read 'Card #D4MV moved to Planned from Linux Chrome | ~ |' (shots sw04b-d)
PASS the desktop's own Switchboard pane, open beside the terminal and read off the window by OCR, counts {'inbox': 1, 'discussing': 3, 'planning': 1, 'planned': 2, 'executing': 1, 'needs-verification': 1, 'needs-qa': 0, 'verified': 0, 'done': 1} — the phone's list {'waiting-on-you': 1, 'inbox': 1, 'discussing': 3, 'planning': 1, 'planned': 2, 'executing': 1, 'needs-verification': 1, 'needs-qa': 0, 'verified': 0, 'done': 1} — and its toast names the move (True); shot sw04d is the picture
PASS + with a title and the request: one new file (2026-09-21-say-which-clock-the-tide-table-uses.md), status 'inbox', the words verbatim under ## Issue — double space, line break and all (True); the phone opened it ('#FPHK'); the desktop's status line read '' (shots sw05a, sw05b)
PASS "zeppelin" is in no card's title ([]) and in #S3ZP's body: the search lists ['S3ZP'] (shot sw06a)
PASS Discuss with the fake model: while it runs the card shows Stop and a working line (True) and the list's row its lamp (['Q2HK']); the answer arrives as an agent's thread entry on the phone (True) and in threads/Q2HK.md (1), and Stop goes away (True) (shots sw07a-c)
PASS Stop on a turn the model would have kept up for two minutes (the fake model saw it 1 time): the phone said 'Stopped.' 0.4 s after the tap and the busy mark went (True) (shots sw07d, sw07e)
PASS Plan on #P7AN: the card's ## Plan changed on the phone (True; it now ends 'on the page.Rewritten by the fake planner (PLANNED-BY-FAKE).') and in the file (True); the card is now 'planned' (shots sw07f, sw07g)
PASS Execute from the phone (the button row was [['Move…', False], ['Execute', False]]): the phone says '#P7AN is executing in a new pane.' with an 'Open pane' link; the desktop's status line read 'Execute on #P7AN from Linux Chrome'; the file is now status='executing' session='4831bb72'… with ['Claimed (4831bb72) · working on it from a terminal pane\n\nExecute pressed on Linux Chrome.'] in its thread; the inbox went 1 → 2 panes; the link opened that pane on the phone (True), where its agent had been handed the card and answered for it (True; the fake model's scenes include EXECUTE:P7AN: True) (shots sw08a-c)
PASS Verify on #V6RF (needs-verification; the row offered ['Move…', 'Verify']): '#V6RF is being verified on Fake in a new pane.', and the verifier's pane was handed the card (True) (shots sw08d, sw08e)
PASS with another window focused (8388640 is not Relay's 2097162) and the desktop's Switchboard pane closed, #R4SK was made to wait on the owner on disk the way an agent does it. The hub handed the push service 1 push; opened with the stub subscription's key and the page's seal key it is {'v': 1, 'kind': 'card_waiting', 'pane': '', 'card': 'R4SK', 'title': 'A card is waiting on you', 'body': '#R4SK'} — the kind, the card's id, no title and none of the question (leaked: []). The phone's row went '1 waiting on you' → '2 waiting on you' and the badge 1 → 2 (shot sw09a)
NOTE the push went to the real rendezvous's /v1/push/send, which answered '/v1/push/send failed: 400 {"error": "that endpoint is not a Web Push service."}': the stub subscription's endpoint (push.invalid) is not a Web Push service, and the hosted rendezvous posts to nothing else. Delivery to a lock screen is the real phone's to show; the body above is what it would be handed
PASS tapping the notification opens that card: posted to the open page as the service worker posts it (True), and opened cold at ?card=R4SK (True, the query dropped again: True) (shots sw09b, sw09c). The tap itself is the one thing stubbed here: a headless page has no notification to tap
PASS 1 socket cut and the page taken offline (link 'offline'): a comment written then reads 'Comment · sends when back online', the strip 'Offline · the board as of 08:56 AM · 1 card change · sends when back online', and it waits in the thread marked as pending (True) with nothing on disk (0); back online ('connected') it is in threads/N2BX.md exactly once (1) and once on the phone (1, no longer pending), the strip cleared ('') (shots sw10a, sw10b)
PASS a guest admitted to one pane as an editor (True) and the view-only device each sent board_request {board_comment} from its own session ({'guest': 'sent', 'viewer': 'sent'}; the hub's tap saw [('guest', 'editor', 'board_comment'), ('device', 'view', 'board_comment')]). On the wire each was answered with an error and nothing else — guest: ["a guest never gets that: the Switchboard is the owner's cards and threads, not the shared pane."], view: ['this device is paired for view.'] — and while the owner's phone then wrote a note, the guest's page decrypted 0 board_event(s) and the view device's 0; of the 98 board_events the hub sent in this run, 0 went to anything but a full device. The audit log has board_refused for both ([{'kind': 'board_refused', 'participant': '13dc0ad197c18c6c', 'code': 'not_permitted', 'type': 'board_comment'}, {'kind': 'board_refused', 'device': 'c100dfe8afa6229a', 'code': 'not_permitted', 'type': 'board_comment'}]) with the type and no text or card (True) (shots sw11a-d)
PASS the phone's page decrypted 303 messages in this run, 94 of them board_events (131251 bytes, kept whole in phone-board-frames.jsonl; kinds ['board', 'board_action_result', 'board_activity', 'board_cancelled', 'board_card', 'board_changed', 'board_search', 'board_thread_appended', 'board_written']): none holds the project's path, the jail's, "/home/" or "/tmp/" (0 hits), none has a path-named key ([]), and the errors, the welcome and push_state have none either (none)
NOTE outside the Switchboard's messages the pane protocol does name this machine's paths, as it did before this card: ['agent conversations', 'panes', 'screen_snapshot'] — `panes[].cwd` is section 6.3's (the inbox's right-hand label) and the rest is the terminal's own picture and the pane agent's events, which a `full` device of the owner's is paired to see. phone-frames-summary.txt has a sample of each
PASS at 1180×820 the board lays itself out for a tablet: the list at x [0, 425] and the open card at x [425, 1180], side by side (True), nothing wider than the screen (True) (shot sw13a)
PASS at 844×185 — a phone on its side with the keyboard up — the reply box (True) and its send (True) are wholly on screen; Discuss (True) and Plan (True) too (shot sw13b)
PASS the run asked the hosted rendezvous for 17 room(s) — a pairing link, a pairing code and an invite are one each — of the twenty an hour it allows a desktop; refused: []
NOTE phone console: ['error: Loading the script \'https://static.cloudflareinsights.com/beacon.min.js/v31edd6df95cf4e85bb4c19e7a9bdbcba1788362987495\' violates the following Content Security Policy directive: "script-src \'self\'". Note that \'script-src-elem\' was not explicitly set, so \'script-src\' is used as a fallback. The action has been blocked.', 'error: Loading the script \'https://static.cloudflareinsights.com/beacon.min.js/v31edd6df95cf4e85bb4c19e7a9bdbcba1788362987495\' violates the following Content Security Policy directive: "script-src \'self\'". Note that \'script-src-elem\' was not explicitly set, so \'script-src\' is used as a fallback. The action has been blocked.']
NOTE guest console: ['error: Loading the script \'https://static.cloudflareinsights.com/beacon.min.js/v31edd6df95cf4e85bb4c19e7a9bdbcba1788362987495\' violates the following Content Security Policy directive: "script-src \'self\'". Note that \'script-src-elem\' was not explicitly set, so \'script-src\' is used as a fallback. The action has been blocked.']
NOTE Relay ended: exit 0
