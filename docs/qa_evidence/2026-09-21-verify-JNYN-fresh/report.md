# Independent review of #JNYN

Review: 2026-09-21 America/New_York (2026-09-22 UTC), Codex verifier a1, separate from implementer. **FAIL — return to Ready for repair.** No implementation edits. Prior interrupted verifier evidence was read only as context and never counted as proof. No Human QA on the real card was changed; all synthetic answers were confined to the disposable fixture.

## Tested revision and isolation

Backend checks used the shared checkout's unchanged Try it Python files, identical to the committed blobs at `0241d05ef19393e84c0ef98655c9687466a92efc` (also unchanged across initial tip `5c4d64f40765`). Exact blob IDs and GUI binary/manifest SHA256s are in `runtime.txt`. The current shared GUI executable could not be reopened during the concurrent rebuild (PermissionError); no competing build was started. UI results instead use `/tmp/claude-1000/land/jnyn/verify/build/relay`, **exploratory only**, identified by its build-gate manifest, not represented as current main or a known commit. Gate Python blobs differ from today's backend checks. Existing `build` cardtests also have unestablished source provenance: passing is supporting evidence, not a clean-revision build result.

Fresh project `/tmp/jnyfresh-5j5c664f`, Xvfb `:283`, separate HOME/config/data/cache/runtime/tmp, `RELAY_KEYRING=off`. Fixture is generated by `python3 docs/qa_evidence/2026-09-21-verify-JNYN-fresh/drive.py stage`; no provider, credentials or network. `launch.py` records the actual launch recipe and reads the staged root from `/tmp/jnyn-fresh-root`. `drive.py backend` reproduces the defects through real BoardCommands/BoardTools with only the turn supervisor stubbed. This does not claim an actual model completed staging.

## Done means checklist

| Item | Verdict | Fresh evidence |
|---|---|---|
| 1. Reuse Verify staged directory; self-stage only if absent | PARTIAL | 32 protocol tests pass, including Verify-directory reuse and prompt branch. No real provider-driven reuse completed in this session. |
| 2. Action from needs-verification; bounded agent reads criteria, stages/plays disposable fixture, captures evidence and hands over one task/question with sealed result | PARTIAL/BLOCKED | `ui.png` shows Try it action and seeded handoff with expected text absent. `ui-answer.png` shows Open it actually ran `printf JNYN-FRESH-OPEN`. `ui-try.png` shows actual Try it click refused for unsupported guest provider. This proves UI wiring on gate binary, not successful agent staging or bounded completion at current main. |
| 3. Staging/mechanical failure leaves report and no review request | FAIL | Ordinary error case passes unit tests, but `backend.txt` shows successful retry has `outcome=done`, `section_written=true`, **`staging_failed=true`**, and stale failure text claiming nothing changed. `_failed_note` searches all historical notes without run boundary. A new seeded section was written during the successful retry. |
| 4. Answer recorded as verdict, reveal follows; Human QA generated once | FAIL with normal-path pass | `fixture-after.md`, `thread-after.md`, `ui-answer.png`: synthetic UI answer recorded as decision; expected text revealed; Human QA generated. `backend.txt`: a pre-existing `Answer: Keep the old layout.` disappears because answer replaces the entire Human QA section. Existing owner decisions must survive. |
| 5. GUI, backend and command-line cards work on repository board | NOT ESTABLISHED | Real isolated GUI interaction and backend protocol probes performed; agent staging across all three card classes on the repository board was not exercised. No passing generalization claimed. |

Failure indicators: seeded brief did not reveal the expected text before answering, and Open it needed no human setup in the running fixture. Autonomous staging behavior remains unverified. The stale-failure and decision-loss findings preclude approval regardless of the outstanding broader checks.

## Tests checklist

| Card Tests item | Verdict | Invocation/evidence |
|---|---|---|
| `tests/test_tryit_protocol.py` (32 cases) | PASS | `PYTHONPATH=backend python3 -m unittest discover -s tests -p test_tryit_protocol.py -v`, 32/32, `tests.txt`. Initial pytest attempt unavailable; unittest ran the same test file. |
| `ctest -R cardtests` | PASS, provenance limited | `ctest --test-dir build -R '^cardtests$' --output-on-failure`, 1/1, `tests.txt`; existing binary was not rebuilt by verifier. |
| Manual real Try it on #7BM4 with configured model | NOT REPRODUCED | Old implementer drive not accepted as independent proof. Fresh `ui-try.png` records actual click on synthetic #T9QA in isolated app, refused by missing supported provider. |

## Findings for implementer

1. Scope failure notes to the current run. Reproduction: start, finish error, start again, write valid Try it, finish done. It currently says staging_failed true and publishes the first run's failure. Expected: success refers only to the successful retry.
2. Preserve prior Human QA questions/answers when recording Try it. Reproduction: seed unrelated answered Human QA, then send try_answer. Current section replacement erases that decision. Expected: update only generated Try it content.

Both are reproducible at the recorded committed backend revision, independent of old screenshots. No code fix was authorized for this verifier. The broader live model run and clean current-main UI validation remain outstanding after repair. Generated board index left to Relay to avoid touching another session's file.
