# 2026-09-25 — #265N cleanup-audit follow-ups: evidence

Card: `#265N` (`.board/planning/2026-09-17-cleanup-audit-follow-ups-judgment-call-dead-code.md`).
Land session `phone-265n`. Delivered by the pane's subagents (a2, a4); the audit evidence was regenerated by the parent session after a4 failed before writing it.

## Contents

| file | what it shows |
| --- | --- |
| `quit-check-xvfb.txt` | Live quit check: GUI under Xvfb with isolated `XDG_CONFIG_HOME`, remote/alwaysOn on, SIGTERM → `remote_shutdown clean=1 ms=985` in the relay log, no surviving sidecar process, no `QProcess: Destroyed while process is still running` warning |
| `try-landed-main.txt` | `land.py try phone-265n --commit HEAD --tests "remoteshutdown\|sharing\|remotesettings\|boardpane"` on `main` at `c926b960` (after a99f092e, d4b6658a, b37a4777 landed): the whole tree builds — `relay-consolemode-tests` included — and all four targeted ctest suites pass |
| `pytest-remote-gui-host.txt` | `python3 -m pytest tests/test_remote_gui_host.py -q` — 52 passed, including the new stdin-EOF-runs-`Sidecar.stop()` pin |
| `engine-alias-build.txt` | Clean `git archive` of `main`, configured with `-DRELAY_BUILD_ENGINE=ON` (the `RELAY_BUILD_ENGINE_SPIKE` alias removed in d4b6658a): `relay-vterm-spike` builds; `rg ENGINE_SPIKE CMakeLists.txt engine docs` (outside qa_evidence) and `rg ENGINE-SPIKE.md docs` are empty |
| `audit-remote-vulture.txt` | `uvx vulture remote/ --min-confidence 80`: 2 hits, both interface-stub parameters (`remote/panes.py:103,106`) — false positives |
| `audit-settings-keys.txt` | Write-only/read-only QSettings key literal scan (`scripts/settings_keys.py`): 0 write-only; 53 read-only, all false positives — the Options tables in `src/RelayWindowSettings.cpp` write them by key without a `setValue("literal")` call (spot-checked `agent/max_steps`, `security/readable_roots`, `window/native_frame`, `voice/max_seconds`, `recap/away`) |
| `audit-engine-uncalled.txt` | Uncalled-function sweep over `GhosttyCore.cpp` / `TerminalSession.cpp` / `TerminalView.cpp` (`scripts/engine_uncalled.py`): 0 candidates |
| `audit-relaywindow-methods.txt` | RelayWindow method sweep (`scripts/relaywindow_methods.py`): 3 confirmed dead (`closedItemForSession`, `openConsoleModelPicker`, `serializeTabs`) — filed as bug #ZR3N |
| `audit-includes.txt` | Include triage over `src/Pane.h`, `src/BoardPane.cpp`, `src/RelayWindow.h` (`scripts/includes_triage.py`): 0 candidates in `Pane.h`/`BoardPane.cpp`; 7 in `RelayWindow.h` (`AuxiliaryZoom.h`, `PromptHistory.h`, `RichEditor.h`, `EscapeeCaps.h`, `Logging.h`, `Aliases.h`) whose names the header itself does not use. Not a fault: the window sources that include `RelayWindow.h` may rely on them transitively, so trimming is a refactor with a compile sweep, not filed |
| `scripts/` | The four report-only audit scripts; each takes a checkout root (`python3 scripts/X.py <git archive of HEAD>`) |

The consolemode compile break first found here (44 errors, `tests/pane_waits.h` never included) was filed as #ZR2M; the include landed with a99f092e and `try-landed-main.txt` shows the target building on `main`, so #ZR2M is closed as fixed.

## Deviation from the card's plan, recorded

The C++ fake-sidecar test lives in `tests/consolemode_test.cpp` (new `--remote-shutdown-only`
filter, `remoteshutdown` ctest) rather than the `sharing`/`remotesettings` suites:
`src/RemoteShare.cpp` is only compiled into the `relay` target and the console-mode targets, so
the console-mode binary is the only existing test target that can drive `RemoteShare::shutdown()`
without dragging the GUI view stack into a settings-only test.
