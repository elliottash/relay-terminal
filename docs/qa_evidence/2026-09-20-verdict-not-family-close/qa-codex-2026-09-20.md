# #76DJ — independent QA verification (codex)

- Verifier: codex (QA pane), 2026-09-20. Implementer was GLM; this is an independent check.
- Scope: the QA checklist on card #76DJ
  (`issues/features/needs_qa_llm/2026-09-19-closing-a-qa-card-needs-a-verdict-not-a-differen.md`).
- Tree: `main` at HEAD, `2f005783` confirmed an ancestor ("Closing a QA card needs a verdict, not a
  different model family (#76DJ)"; touches board_tools.py, board.py, board_policy.md, both
  SWITCHBOARD docs, the card, tests/test_board_tools.py). The shared checkout carries unrelated
  uncommitted WIP (priority-flag work, ~63 files); none of it intersects the close gate —
  `git diff backend/relay_core/board_tools.py` has hunk headers at 1491/1792/1963 only, no diff
  line mentions verdict / independent_model / verified_by / family, and `board_policy.md` is
  unmodified. Python 3.12.3. No source files were edited by this verifier.

## Checklist findings

### 1. Both suites pass with the expected counts — PASS

- `PYTHONPATH=backend python3 -m unittest tests.test_board_tools` → `Ran 148 tests … OK`.
- `PYTHONPATH=backend:tests python3 -m unittest tests.test_board_protocol tests.test_tool_labels
  tests.test_board_import` → `Ran 258 tests … OK` (148 + 110 = 258).
- Both re-run 3× sequentially: OK every time.
- Observation (not a failure of the item): one transient run of the combined suite produced 137
  errors + 1 failure when it executed as a second Python process *concurrently* with the other
  suite; it never reproduced afterwards (3 sequential reruns, 1 deliberate concurrent rerun, and
  the first clean run all OK — the two suites share live board/worker state and interfere under
  concurrency). Run the two commands one at a time, as the checklist specifies, and they are
  deterministically green.

### 2. Same-family close succeeds and stamps verified_by — PASS

- `tests/test_board_tools.py:625` `test_the_same_model_family_closes_it_once_the_verdict_is_there`
  (comment: "Owner, 2026-09-20 (#76DJ): the verdict is the gate, not the closer's model family"):
  card implemented by `anthropic/claude-opus-5`, `## Verdict` appended, closed to `done` from the
  same family → asserts no error, `status == "done"`, and
  `front["verified_by"] == "anthropic/claude-opus-5"` (lines 627–636). Passes.
- The gate it exercises is real: `backend/relay_core/board_tools.py` (~1907–1927 in the working
  tree) closes out of a QA lane with only (a) a verdict-family section heading check
  (`requires: verdict`), (b) the Relay-Free refusal (`requires: independent_model`, "Verifying is
  not available on Relay Free (owner's decision, 2026-09-19)"), then stamps `verified_by` with the
  closer. No comparison between the closer's family and `implemented_by` remains.
- Companion flip `test_relay_free_cards_close_like_any_other_once_verified` (line 732): a
  relay-free-implemented card closed by `glm-coding` → `verified_by == "glm/glm-5.3"`. Passes.

### 3. No-verdict and Relay-Free refusals still refuse — PASS

- `test_closing_a_qa_card_needs_a_verdict_section` (line 618): close without a verdict section →
  `result["requires"] == "verdict"`, status stays `needs-qa-llm`. Passes.
- `test_relay_free_may_not_close_a_card_at_all` (line 747): a *different-family* Relay-Free closer
  with the verdict present → `requires == "independent_model"`, error contains
  "not available on Relay Free", status stays `needs-qa-llm`. Passes.
- Precision on "both relay-free refusals": after 2f005783 exactly one Relay-Free refusal test
  remains (this one). The file had three `requires: independent_model` assertions before the
  commit and has one after — the same-family-close and relay-free-upstream refusals were the two
  intentionally flipped to success tests. The single refusal the new rule keeps, keeps refusing.

### 4. No stray promise of the old rule — FAIL (narrow, comment-only)

`rg -n "independence|must not be the family" backend/ docs/SWITCHBOARD-DESIGN.md
docs/SWITCHBOARD-FORMAT.md` → 4 hits, all in `backend/`, **zero in the two docs**:

- `backend/relay_core/board_tools.py:352` — historical, past tense ("Before that … the
  independence rule let each close what the other wrote"): the #T71W history note the card
  anticipated. Fine.
- `backend/relay_core/qa_verifiers.py:244` — **a live, present-tense promise of the removed
  rule**: "What does NOT rest on this is the rule that a different family closes a QA card: that
  is a process-independence guarantee for the audit trail and holds whatever the error
  correlations turn out to be." 2f005783 did not touch qa_verifiers.py, so this line in the
  #T71W lineage dossier still states the old close rule as standing policy.
- `backend/relay_core/qa_verifiers.py:13` — stale rationale: "Both spellings must land on the
  same family, or the independence rule would let Claude close what Claude wrote." The stated
  close-gate consequence no longer exists (spelling unification still matters for the
  `recommend()` skip, which is live).
- `backend/relay_core/qa_verifiers.py:143` — "the independence rule keeps working on a provider
  this table has never heard of": defensible as the still-live *recommendation* independence
  (`recommend()` still skips the implementer's own family), not the close gate.

No runtime effect anywhere (all three are comments/docstrings); every enforced rule matches the
new policy. But the checklist item as written — "no live promise of the old rule survives" — is
not met: `qa_verifiers.py:244` (and secondarily `:13`) still promise it in prose. A one-line
comment fix / follow-up card would close this out.

### 5. Policy rule 5 and the tool description state the new rule — PASS

- `backend/relay_core/board_policy.md` rule 5: "Closing a QA card needs the verifier's verdict in
  the body — any pane may flip it once that is there (owner, 2026-09-20) — and the card's `qa`
  recommendation still names the best verifier." (Plus: Relay stamps `implemented_by` /
  `verified_by`, so agents never type them.)
- `board_move_card` description (`board_tools.py` ~216–221): "Closing a card that is in a QA lane
  requires a verdict section in the body — any pane may flip it once the verdict is there; Relay
  Free still may not."

## Overall verdict

**PASS with one caveat.** The rule change is complete and correct everywhere it is enforced or
loaded: both suites green at the expected counts (148 / 258), the same-family close succeeds and
stamps `verified_by`, the no-verdict and Relay-Free refusals refuse exactly as before, and rule 5
plus the tool description carry the new wording (items 1, 2, 3, 5 — PASS). Item 4 fails narrowly:
a comment-only leftover in `qa_verifiers.py:244` (secondarily `:13`) still asserts the old
different-family close rule in the present tense. No behavior depends on it; recommend a small
follow-up to reword those two comments.

## Card writes

This verifier has no `board_update_card` / `board_move_card` tools in its toolset, so per the QA
instructions no writes were attempted on #76DJ, #EV45 or #MERX; all three cards are left as they
were for a pane that holds the board tools.
