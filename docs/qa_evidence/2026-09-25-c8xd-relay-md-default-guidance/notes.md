# #C8XD evidence — 2026-09-25

## Dogfood on this repo
$ python3 scripts/relay-board.py --board .board policy
wrote .board/POLICY.md / RELAY.md / CLAUDE.md / AGENTS.md

CLAUDE.md and AGENTS.md now hold only the generated note:
<!-- relay:switchboard-policy start -->
@RELAY.md — this project's default agent guidance: the working rules and the Board policy.
<!-- Generated by Relay (relay_core.board.note_text): this block is replaced whenever the
     board scaffold runs. Edit around it, not inside it. -->
<!-- relay:switchboard-policy end -->

RELAY.md (the renamed working-rules file) gained the Board policy block at the end,
hand-written content untouched: 87 lines total.

## Tests
$ PYTHONPATH=backend python3 -m unittest tests.test_board tests.test_session_protocol
Ran 197 tests in 24.167s

FAILED (failures=1)

## Fresh regeneration of .board/POLICY.md
The dogfood run also refreshed `.board/POLICY.md`, which was stale on main (it still said
`issues/`); #WC3E's test demands it be committed with what made it stale, so it lands here too.
The same test now reads the board this checkout actually uses (`B.board_folder(repo)`) instead
of the legacy `issues/` folder, so a leftover gitignored legacy board cannot fail the suite.

Final: PYTHONPATH=backend python3 -m unittest tests.test_board tests.test_session_protocol —
197 tests, OK.
