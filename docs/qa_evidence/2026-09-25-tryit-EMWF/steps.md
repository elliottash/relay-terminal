The user's path (numbered; each marked check / agent / person):

1. check — `stage.sh` runs and prints three Issue sections (asserted by the script's own exit
   status and the capture `01-three-issues.txt`; the OLD WAY card is a bare quote, the NEW WAY
   card carries summary + attributed session-linked quote, the no-quote card carries a summary
   alone). A test decides this: no person needed.
2. agent — the capture was taken and read by the staging agent (this file's sibling,
   `01-three-issues.txt`). Mechanical; done.
3. person — read the three Issue sections and answer the one question on the card. Only a person
   can judge whether the format reads right to the person it quotes: whether the summary is the
   kind of summary they asked for rather than a re-worded quote, and whether the attribution
   (name · session link · date) tells them at a glance who said it and where. ~1 minute.

Counts: 1 check, 1 agent, 1 person.
