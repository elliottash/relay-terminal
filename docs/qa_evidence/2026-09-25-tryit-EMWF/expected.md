Expected result (sealed until the person answers):

- The card titled "OLD WAY: scratch watcher flags the guest harness home" has an Issue that is
  just the user's words, with no summary, no quote mark and no attribution — the shape the
  owner disliked in #WZ3K.
- The card titled "NEW WAY: voice transcription mode" opens with the agent's one-line summary
  ("Voice-to-text in the composer: a microphone icon…"), then the user's words on `>` lines
  exactly as typed, then one attribution line: `— elliott · session:0f3ac2de…` shown as a link
  whose label is the full session id, `· 2026-09-25`.
- The card titled "NEW WAY, no quote: a fault the agent noticed" has the summary alone — no
  quote block — because it quotes nobody.
- The judgement asked of the person: the summary is a real description (what and why), not a
  restatement of the quote; the quote is unmistakably the user's own words and the attribution
  says who, where and when at a glance.
