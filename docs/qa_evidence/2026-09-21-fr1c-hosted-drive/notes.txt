PASS a fresh profile starts with remote control off (True, no [remote] section: True); the plug menu's first item opens the pairing dialog (True), which turns the service on by itself: the sidecar runs (True), the desktop registers at join.relay-terminal.ai (health desktops 1 → 2), relay.conf remembers alwaysOn=true (True) with address=relay-terminal.ai (True), the dialog shows a code shaped 'ABCD 4829' (True) and the link is hosted (https://join.relay-terminal.ai/pair#…, 135 chars) (shots step14a-e)
PASS the welcome screen at https://join.relay-terminal.ai/ leads with 'Enter the code from your desktop'; the eight characters from the dialog pair this phone: the same five digits show on both sides ('91690' — shot step15c is the desktop's), Allow typing lands it in the inbox (True, one screen drawn: True) as 'full', link 'connected'; the rendezvous's /v1/health and the desktop's audit carry neither the PIN nor the pairing secret (True), and the PIN is in no console line (True). The desktop's audit has code_used for PBDT exactly once with no failed try (True), and the dialog's code row ended: it offers "New code" (True; shot step15e reads "Used") (shots step15a-e)
NOTE the inbox led with the notifications offer on arrival: True (step 18 taps it)
NOTE the third wrong PIN was answered with 'That PIN is not right … then try again' — the served meet.js predates the fix in the repo, which makes it say the code has stopped working; it needs the redeploy
PASS three wrong PINs on a fresh code (VGNX) burn it: the page counts them (['That PIN is not right. Wrong PINs count: after three, this code stops working. Check the four digits on your desktop, then try again.', 'That PIN is not right. Wrong PINs count: after three, this code stops working. Check the four digits on your desktop, then try again.', 'That PIN is not right. Wrong PINs count: after three, this code stops working. Check the four digits on your desktop, then try again.']), and the *right* PIN afterwards is refused — 'There is no pairing code with those letters. Codes last ten minutes — if the one on your desktop is older than that, choose “New code”.' — with the phone still on the welcome screen (True). The desktop's audit has code_burned with failures=3 (True) and the dialog's row reads "Closed" with a "New code" button (shot step16b). That button (pressed) mints AGQY, which pairs a second device (True) (shots step16a-e; the old code was PBDT)
PASS a pairing link opened in Safari on an iPhone that is not installed shows the install card (True) and hands the link over instead of pairing: screens ['screen-welcome'], the copy row is up (True) with the link itself (True), and ten seconds later nothing has paired (True) — the desktop was never asked: its audit has 2 pairing(s) before the tab and 2 after, and shot step17b is the dialog with no approval row. The same link in an installed app pairs (True, left at 'https://join.relay-terminal.ai/'; the audit now has 3), which is the proof the tab never spent it (shots step17a-c). Lead: 'Add Relay to your Home Screen first, then open it from there and\n          enter'
PASS the first arrival after pairing leads with one button: {'top': True, 'offer': True, 'text': 'Turn on notifications', 'first': True, 'permission': 'default'}; one tap turns both switches on ([['notify-switch-finished', True], ['notify-switch-needs', True]]) and the offer collapses (True), leaving the settings row ('Stop notifying this phone') where it is changed afterwards (shots step18a, step18b). The subscription itself is the browser's one stub (pushManager.subscribe); everything on the desktop's side of it is real.
PASS the plug menu's switch turns remote control off: the rendezvous drops the desktop (desktops 1), the phone says 'offline', alwaysOn=false is written (True). Turned on again from the same menu the desktop is back (desktops 2, alwaysOn=true: True) and the phone reconnects (on its own) straight into the inbox with 1 pane(s) and no pairing (True, screens ['screen-inbox']) (shots step19a-e)
PASS every code the dialog showed stayed the code: 5 minted (['PBDT', 'VGNX', 'AGQY', 'DBYM', 'EWMW']), none ended by the service's move to join.relay-terminal.ai (code_expired: []) — see audit-steps-14-19.txt
NOTE phone console: ['error: Loading the script \'https://static.cloudflareinsights.com/beacon.min.js/v31edd6df95cf4e85bb4c19e7a9bdbcba1788362987495\' violates the following Content Security Policy directive: "script-src \'self\'". Note that \'script-src-elem\' was not explicitly set, so \'script-src\' is used as a fallback. The action has been blocked.']
NOTE Relay ended: exit 0
NOTE the profile was wiped between the two halves: the thirteen steps below run on a fresh desktop identity, with the same build, jail and fake model
PASS a fresh profile starts with remote control off: Relay's children after 14 s are ['/usr/bin/python3 -S -u /home/elliott/repos/relay-terminal/ba', '/bin/bash --noprofile --rcfile /home/elliott/repos/relay-ter'] (no gui_host sidecar); relay.conf has no [remote] section: True; join.relay-terminal.ai lists 1 desktop(s) online before this run (shot 01)
PASS Options › Remote's switch starts the sidecar (True), the desktop registers at join.relay-terminal.ai (health desktops 1 → 2), and relay.conf remembers remote/alwaysOn=true (True); the address row is the default, relay-terminal.ai (shots 02, 03, 03a)
PASS the pairing link points at the hosted app: https://join.relay-terminal.ai/pair#… (135 chars)
NOTE the phone shows 48566; shot 07 is the desktop's — compare them
PASS the phone paired with Allow typing and reads 'full', link 'connected' (shot 08)
PASS the phone holds a connect token (35 chars) for desktop 1adefb42… (the record's id matches the link's key: True)
PASS a connect with no token at all to the paired desktop is refused: closed 4404 'no such desktop or the pairing code expired.'
PASS a connect with a made-up token to the paired desktop is refused: closed 4404 'no such desktop or the pairing code expired.'
PASS the inbox lists both panes with no share button pressed: 2 rows, chips ['idle', 'idle'] (shot 09)
PASS a pane opened on the desktop appears in the inbox by itself: 3 rows (shots 10, 11)
PASS a prompt sent through the pane view’s box (one screen drawn: True) ran on the desktop: the chip read 'running · 0s' then 'finished', and the fake model's answer reached the terminal (True) (shots 12-16)
PASS Stop is in the strip while the turn runs (True, clock 'Relaying · thinking… · 3 s · step 1/500 · Esc stops') and one tap ends a turn the model would have kept up for two minutes (True, in 0.4 s) (shots 17-19)
PASS the worker's ask_user is drawn on the phone (True: {'header': 'Which branch', 'text': 'Which branch should the fix land on?', 'choices': ['main', 'a side branch']}), a tap on a choice closes it (True) and the model got the answer (True) (shots 20-23)
PASS Recap from the pane menu prints a recap in the pane (True) with no error ({'term': 'Read only until you take over.', 'thread': '', 'toast': ''}) (shots 24-26)
NOTE the recap's side call reached the fake model 1 time(s); 0 means the summaries role resolved to another provider (see README, "what is real")
PASS 1 socket cut and the page taken offline (link 'offline'); a prompt typed then reads 'Sends when back online · 1 message'; back online ('connected') it arrived (True) exactly once (the model saw 1 request(s) for it, the same as for step 5's prompt: 1); the note is cleared afterwards ('') (shots 27-29)
PASS the Notify row ({'text': 'Notify me on this phone', 'note': '', 'permission': 'granted'}) subscribes (push service stubbed in the browser) and the desktop answers with the two switches: [['notify-switch-finished', True, 'agent_finished failed plan'], ['notify-switch-needs', True, 'waiting_input password']]; the row says 'Your desktop decides what is worth telling you, and a notification never carries what is on the screen.' (shots 30, 31). Delivery to a lock screen is Phase 3, on the real phone
NOTE after turning the second switch off: [['notify-switch-finished', True], ['notify-switch-needs', False]] (shot 32)
PASS the share window opened later in the run lists the paired phone: True (shot 33; with remote control on the list was reported at launch, before any window existed — RemoteShare keeps it now)
PASS an editor invite was made on the desktop, through the hosted address: https://join.relay-terminal.ai/join#…
PASS the knock reaches the owner's phone with the same code the guest sees (55428): 'alice wants to join as editor · code 55428AdmitRefuse' (shots 35-37)
PASS Admit on the phone lets the guest in (shots 38, 39)
PASS a guest prompt waits on the desktop, shows on the phone ('alice: GUESTQ what is in this folder?RunRefuse'), Run sends it to the agent (True) and the model answered it (True) (shots 40-44)
PASS Relay quit cleanly on SIGTERM (exit 0, gui_crash lines: 0); the rendezvous shows it gone (desktops 1); the phone says 'offline' (shot 45)
PASS after a restart on the same profile the sidecar comes back by itself (True), the desktop is registered again (desktops 2), and the reloaded phone is straight in the inbox with 1 pane(s), no pairing (['screen-inbox']; link 'connected'; welcome note '') (shots 46, 47)
PASS a paired phone refreshing the tab it paired in (/pair, no code) lands in 'the inbox' (shot 47a)
PASS the switch off withdraws the desktop from the rendezvous (desktops 1), the phone says 'offline', and alwaysOn=false is remembered (True); the sidecar is still a child of Relay (shots 48, 49)
NOTE phone console: ['error: Loading the script \'https://static.cloudflareinsights.com/beacon.min.js/v31edd6df95cf4e85bb4c19e7a9bdbcba1788362987495\' violates the following Content Security Policy directive: "script-src \'self\'". Note that \'script-src-elem\' was not explicitly set, so \'script-src\' is used as a fallback. The action has been blocked.', 'error: Loading the script \'https://static.cloudflareinsights.com/beacon.min.js/v31edd6df95cf4e85bb4c19e7a9bdbcba1788362987495\' violates the following Content Security Policy directive: "script-src \'self\'". Note that \'script-src-elem\' was not explicitly set, so \'script-src\' is used as a fallback. The action has been blocked.', 'error: Loading the script \'https://static.cloudflareinsights.com/beacon.min.js/v31edd6df95cf4e85bb4c19e7a9bdbcba1788362987495\' violates the following Content Security Policy directive: "script-src \'self\'". Note that \'script-src-elem\' was not explicitly set, so \'script-src\' is used as a fallback. The action has been blocked.']
NOTE guest console: ['error: Loading the script \'https://static.cloudflareinsights.com/beacon.min.js/v31edd6df95cf4e85bb4c19e7a9bdbcba1788362987495\' violates the following Content Security Policy directive: "script-src \'self\'". Note that \'script-src-elem\' was not explicitly set, so \'script-src\' is used as a fallback. The action has been blocked.']
NOTE Relay ended: exit 0
---- by hand, after the run (a re-run rewrites everything above this line and drops what is below; README.md keeps the same list) ----
ODD  the first three runs' audit had code_create / code_expired 1.5 s later / code_create: the dialog's first code was minted before the sidecar reached join.relay-terminal.ai and swapped under the reader's eyes. Fixed in 15b0340f (remote/gui_host.py); the "every code the dialog showed stayed the code" line above is the live check.
ODD  the third wrong PIN is still answered "…then try again" by the deployed page (NOTE above). Fixed in 494a750a (app/meet.js); needs the redeploy.
ODD  served pane.css and meet.js differ from the repo (provenance.txt): 831470e8 and 494a750a are not deployed yet.
ODD  /v1/health counted one desktop that is not this run's throughout (the owner's own Relay, DISPLAY=:0, remote control on): counts read 1 -> 2 -> 1, not 0 -> 1 -> 0. After the run: 1, which is his.
ODD  one start of the drive was refused by its own precheck: /v1/health did not answer within 5 s, once, at about 22:17; it answered 0.3 s later by hand. Nothing was started and nothing was written.
ODD  scripts/relay-build does not compile at 494a750a (another session's unfinished exportProfiles in src/RelayWindow.h), so the binary is the 22:04:03 build at ad8beded, run from a private copy.
ODD  the predecessor's second run restarted Relay (step 12) on a binary another session had rebuilt seven minutes into the run; hence the private copy.
ODD  every page load logs a CSP refusal of Cloudflare's injected analytics beacon (the NOTE lines above). Owner's Cloudflare dashboard.
ODD  step18a: "Turn on notifications" at the top and "Notify me on this phone" at the bottom, the same act twice on one screen; pinned as deliberate by tests/test_remote_browser.py.
ODD  the dialog counts the QR in seconds ("The QR lasts 299 s.") and the code in minutes ("Expires in 9:58").
ODD  step17a and every QR here show a whole pairing link: one-time, spent or expired within five minutes, for a desktop identity that was deleted with the jail.
ODD  relay-stderr*.log: "QProcess: Destroyed while process (\"python3\") is still running." once per Relay exit — the sidecar is still alive when RemoteShare's QProcess is destroyed at quit, and Qt kills it. The rendezvous sees the desktop go at once (step 12 and step 19 pass), and the #PH0N drive's logs have the same line; not new, not chased here (src/RemoteShare.cpp is C++ and the shared tree does not compile tonight).
