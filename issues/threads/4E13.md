<!-- relay:entry 20260919T013237Z-tz author=agent kind=event model=glm-5.3 pane=e3eb120b turn=b4923f56f3cb458da1e5f295f29222bc/31268b9f016c4fe8b06a35c1ce91a2d7 -->
- ✦ agent created this card in In progress · issues/features/2026-09-18-say-relaying-while-an-agent-turn-runs-not-workin.md

<!-- relay:entry 20260919T045100Z-rw author=owner kind=event pane=switchboard -->
- ✦ owner updated this card · replaced `## Issue`

<!-- relay:entry 20260919T045100Z-rx author=owner kind=rewrite pane=switchboard -->
- ✦ rewrote ## Issue

<details><summary>before</summary>

```
add "relaying..." when working rather than "working" or "warping"
```

</details>

<details><summary>after</summary>

```
add "relaying..." when working rather than "working" or "warping"

this is good but it should be in bold at the bottom right above the prompt, like in codex etc. it should say Relaying and in purple if agent work, blue if terminal program. 

in the pane header, it should be a blue blinking relay icon (terminal program running) or purple blinking relay icon (agent working). gray or black is idle. green is done, amber/orange is needs human input? 

use this to think about the broader visual l anguage / status indicators. in the terminal. questions or items needing human response could be in bold amber. when the agent is saying they are done, that could be in bold dark green. etc.
```

</details>

<!-- relay:entry 20260919T045102Z-pq author=owner kind=comment mode=plan -->
Plan this card.

<!-- relay:entry 20260919T045220Z-sx author=owner kind=comment mode=plan -->
Plan this card.

<!-- relay:entry 20260919T070001Z-r4 author=agent kind=progress model=claude-opus-5 -->
Implemented and verified. The busy line is a new painted row above the composer (`PaneBusyLine`, `src/Pane.h`): violet "Relaying <action>… · N s · Esc stops" for agent turns (action from the live tool call, "thinking" between calls, "waiting for N subagents…" when blocked), blue "Relaying <program>…" for terminal work, hidden when idle; it replaces the old under-prompt turn clock. The header's live states now draw the symbolic relay mark (`paintRelayMark`, `src/PaneChrome.h`) with a two-step blink (`pulseScale` {1.0, 0.62} in `src/PaneStatus.cpp`); news states keep their shapes. Transcript labelled bolds: done green `1;32`, need amber `1;33`, problem stays red `1;31` (`src/MarkdownAnsi.h`) — amends #CVHT's colours, cross-linked, its text untouched. Tests updated (markdownansi, panestatus, pulsepaint); `ctest` 52/52 that ran, `backend-and-bash` and `./scripts/test.sh` skipped at the owner's instruction after a git-credential prompt hang — environment, not assertions. Live Xvfb evidence in docs/qa_evidence/2026-09-19-relaying-status-language/ (four scenes plus per-word label colours in relay-dark and ibm-beige, blink frame-diffs; see README.md and analysis.txt). Card moved to needs_qa_llm; QA by a non-Claude model.
