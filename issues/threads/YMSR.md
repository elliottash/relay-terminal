<!-- relay:entry 20260919T054321Z-ae author=agent kind=note model=deepseek/deepseek-v4.1-flash pane=f6211bce turn=ec47a91f72694f5c96693d2e0bc7c806/be5fe8343eeb4befb31729b019d9f910 -->
**Implemented, tested and evidenced in the working tree; NOT committed, and why.**

Everything the card describes is in `/home/elliott/repos/relay-terminal` and verified there: `ctest -R panestatus|pulsepaint` passes, and the live evidence is in `docs/qa_evidence/2026-09-19-subagent-badge-in-pane-header/` (scenes none / one / two / busy / endedlive / ended; `analyze.py` finds no badge at all with none running, a 36 px violet chip with 24–37 px of agent-violet ink at x 183..219 beside "Subagents working" for 1 and 2, the same chip beside "Relaying…" while a turn runs, and no chip once the agent finishes — so the count is live, not a session total).

The commit is blocked, and it is not this card's code that blocks it:

- My hunks live in files that also carry card #V8KT's **uncommitted** live-states code. Measured: `git show HEAD:src/PaneStatus.h | grep -c 'pulseScale\|stateText'` → **0**, likewise for `src/PaneStatus.cpp`, `docs/ARCHITECTURE.md` and `tests/panestatus_test.cpp`. The badge is built directly on that work — `PaneSubagentBadge` sits beside #V8KT's `PaneStateWord`, `subagentBadgeStyle` calls their `atLeast`, my ARCHITECTURE paragraph follows theirs, my test cases are in their test file.
- So committing my hunks alone would not compile, and committing the files whole would publish another session's in-flight work, which this checkout's rules forbid (`CLAUDE.md`, "Never commit, stash or revert a file you did not write"). The commit therefore follows whichever session lands those files; this card stays `in-progress` (not `needs-qa-llm`, because it has not landed) with the code in the tree for anyone to read.
- Not mine to touch, but observed: the **shared git index holds 221 paths staged** (index vs HEAD, e.g. `CMakeLists.txt`, `app/*`, `backend/*`, `src/*`, `tests/*`) that no session in this pane staged — an `git add -A`-style sweep. Do not commit from that index (`CLAUDE.md` step 1); nothing was reset here, because a session may be mid-commit.
- Also seen while checking the board: `issues/changes/2026-09-19-configure-protocol-error-nameerror-opaque.md` (another session, filed minutes ago) fails `relay-board.py check` three times — `id '4OSN'` is not Crockford base32 (`O` is excluded; corrected to `40SN` on 2026-09-19), `rank 'zzzz110'` ends in `0`, `status 'open'` is not a work status. Left alone as their card.
