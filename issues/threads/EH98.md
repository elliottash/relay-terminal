<!-- relay:entry 20260919T210723Z-rz author=owner kind=event pane=switchboard -->
- ✦ owner created this card in Inbox · issues/features/2026-09-19-word-wrap-in-thinking-bubbles.md

<!-- relay:entry 20260919T210747Z-f1 author=owner kind=event pane=switchboard -->
- ✦ owner updated this card · replaced `## Issue`

<!-- relay:entry 20260919T210747Z-f2 author=owner kind=rewrite pane=switchboard -->
- ✦ rewrote ## Issue

<details><summary>before</summary>

```
word wrap in thinking bubbles
```

</details>

<details><summary>after</summary>

```
text is not wrapping at word boundaries in the agebt thinking bubble. that should be fixed to make it more readable.
```

</details>

<!-- relay:entry 20260919T210754Z-r8 author=owner kind=comment mode=plan -->
Plan this card.

<!-- relay:entry 20260919T211306Z-fd author=agent kind=event model=glm-5.3 pane=switchboard turn=55123e49778a444dabe580b477a68878/914b48e8db2c491189e132909bc1d409 -->
- ✦ agent updated this card · replaced `## Plan`

<!-- relay:entry 20260919T211310Z-vv author=agent kind=comment mode=plan model=glm-5.3 turn=55123e49778a444dabe580b477a68878/914b48e8db2c491189e132909bc1d409 -->
The remote pane has one bubble; the desktop pane has its own overlay styled in `Theme.cpp`. Let me look at the desktop pane's bubble code in `Pane.h`.

The desktop pane shows thinking as a fold in the terminal, while `RemotePane` has a real "reasoning bubble" widget. Let me check what bubbles exist on the desktop side (`PaneStatus.cpp`, `Pane.h` bubble code) to pin down which surface the card means.

`Theme.cpp` in the current tree has no thinking overlay — the desktop shows thinking as a fold in the terminal. Let me see what else uses the bubble machinery in `Pane.h` and how the thinking text is produced and wrapped.

`Pane.h` is too large to read whole. Let me pull the key regions: `CallLines.h` (the thinking fold), and `PaneState.cpp` (how the tail is built).

The tail is what feeds the remote "reasoning bubble". Now let me check the phone/web app side (`remote/app`) and how the fold's rows are wrapped, to find where wrapping at word boundaries is missing.







The phone app has the bubble: `app/pane.css` `.rp-thinking-tail`. Let me read that stylesheet region.

The phone bubble uses `white-space: pre-wrap; overflow-wrap: anywhere`. Let me check the other candidate surfaces: `app/style.css` `.thinking` and how the Qt-side fold pre-wraps (`foldForMarkdown`), since the desktop's reasoning is a fold now.

Two suspects left: `remote/pane_state.py`'s `_text` (which may hard-wrap the tail at a fixed column) and `foldForMarkdown`'s pre-wrap. Let me read `pane_state.py`.

`_text` keeps newlines and doesn't hard-wrap — so the phone tail relies on CSS. Now the last and most likely suspect: `foldForMarkdown`'s pre-wrap to `cells`. Let me read `CallLines.cpp`.

`foldForMarkdown` delegates pre-wrapping to `wrapFoldLines`. Let me find it.





`wrapFoldLines` hard-wraps at exact cell counts — grapheme by grapheme, no word boundaries. That is the bug. Let me confirm the view's own wrap (`FoldLayer::layout`) does the same, and check whether `MarkdownAnsi` also wraps.

The terminal's own prose already goes through a word wrapper (`Pane.h:11367`). Let me see what that wrapper is, so the plan can reuse it.

`src/WordWrap.h` is the ANSI word wrapper the prose path uses. Let me read it and the fold test that pins `wrapFoldLines` to `FoldLayer::layout`.

One last check — what `docs/ENGINE.md` says about fold wrapping, so the plan covers the doc update.







I have the full picture now. Writing the plan to the card.

Plan written to #EH98. The fault is in the fold layer's shared row-wrap — `relay::wrapFoldLines()` (`engine/TerminalBackend.h`) and `FoldLayer::layout()` break lines grapheme-by-grapheme at the exact cell limit, so the terminal's `▸ ✦ thinking…` fold splits words mid-way while every other prose surface (the pane's own `WordWrap`, the phone and remote bubbles) already wraps at words; the plan makes both wrap points word-aware through one shared helper, with the wrap↔layout agreement tests extended. One thing to confirm before **Execute**: the card says "bubble", but the phone and remote bubbles have no wrapping fault — if you meant one of those rather than the terminal fold, say so and I'll re-plan.
