<!-- relay:entry 20260920T180002Z-c3 author=claude-code kind=event -->
### Claude Code · 2026-09-20 18:00
created this card in Planned from the #PF4K profile: measured findings and the proposed fix; evidence in docs/qa_evidence/2026-09-20-perf-profile/

<!-- relay:entry 20260920T190300Z-p1 author=claude-code kind=progress -->
### Claude Code · 2026-09-20 19:03
Claimed (land session `pf-board`) · implementing the three fixes: `board_search` in the worker so rows stop carrying each card's text, a chunked `board_open` with the worker's failure shown in the pane, and a stat-cached `_rows()`/`check()` so a refresh re-parses only what changed.

<!-- relay:entry 20260920T193000Z-p2 author=claude-code kind=progress -->
### Claude Code · 2026-09-20 19:30
Landed. `board_search` answers the filter's plain words in the worker, the rows dropped `text`, `board_open` and `board_changed` travel in batches (`board_cards`), a worker failure is in the pane with a Retry, and `Board.check()` was split so a refresh reuses what `_rows()` parsed behind a per-file stat cache.

At 353 cards: 7,107 → 653 B a card, `board_open` 309 → 204 ms, `board_refresh` 192 → 6 ms, `board_search` 0.5–1.1 ms. At 3,000 cards: biggest message 21.2 MB → 0.27 MB (the 8 MiB cliff is gone by construction), refresh 1,639 → 55 ms. Evidence and harness: docs/qa_evidence/2026-09-20-perf-fixes/board/README.md.

Tests: `tests/test_board_protocol.py` (search semantics pinned against the old `Model::matches`, batching, and parse counts — 0 files parsed on an unchanged refresh), `tests/boardmodel_test.cpp`, and a new `tests/boardfilter_test.cpp` / `boardfilter` target for the pane. In needs-verification with a QA checklist because it changes a protocol and a visible surface.
