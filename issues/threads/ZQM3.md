<!-- relay:entry 20260921T115610Z-db author=agent kind=progress -->
The follow-up the owner asked for on 2026-09-20 ("moved and put below the x (so it was a sideways L)") was written but left uncommitted in the shared checkout. Landed unchanged on 2026-09-21 as `df67acaf` after land.py's build gate built the exact tree (owner: clean the tree). `PaneChrome`'s own layout is now a QVBoxLayout whose first row is the button row (`buttonRow()`, which `RelayWindow::syncChrome` already reads either shape of) and the share button hangs under it, right-aligned — the L's foot. The screenshots of it are in docs/qa_evidence/2026-09-20-pane-header-two-line-title-and-l-share/. No unit test covers chrome layout; the build gate is the only automated check this one got.
