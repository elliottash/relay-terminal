<!-- relay:entry 20260920T180002Z-c3 author=claude-code kind=event -->
### Claude Code · 2026-09-20 18:00
created this card in Planned from the #PF4K profile: measured findings and the proposed fix; evidence in docs/qa_evidence/2026-09-20-perf-profile/

<!-- relay:entry 20260920T190500Z-a1 author=claude-code kind=progress -->
### Claude Code · 2026-09-20 19:05
claimed this card (#PF4K fix wave, engine area). Implementing all four findings in `engine/` — the
per-row cwd lookup and `hyperlinkAt`, the frame pacing, the full-width scrollback line, and the fold
anchor repaint gate — plus #PPR4's item 2 (`resolveFoldAnchors` / `retainAnchored`), which lives in
the same files. Before/after with the profiler's own harness; evidence under
docs/qa_evidence/2026-09-20-perf-fixes/engine/.

<!-- relay:entry 20260920T203000Z-b2 author=claude-code kind=evidence -->
### Claude Code · 2026-09-20 20:30
landed b8e91fe3 (engine only; `src/` untouched). All four findings plus #PPR4 item 2. Before/after
on spark against the same tree with and without the commit's hunks, harness and numbers in
docs/qa_evidence/2026-09-20-perf-fixes/engine/: GUI CPU per 50 MB of `cat` 1.18 -> 0.65 s, paints
167 -> 60 a second, GUI-thread statx 6 597 -> 494 a second, scrollback 3.01 -> 1.67 kB a line,
`cat` at 281x72 26.4 -> 32.7 MiB/s, `seq 1 2000000` 2.36 -> 1.21 s, byte -> paint p50 5.2 -> 4.8 ms.
Six new engine tests assert the counts (one directory resolution a frame however many link rows are
painted; 2.0 rows a frame with a fold open where it was 10.0; 9 cells converted a stored line, not
120; a second hyperlinkRuns walk visits the screen only; one layout rebuild per resolve of 200
folds). `engine/scripts/gui/folds.sh` gives pixel-identical screenshots in all eight fold states.
Two corrections are on the card: finding 4's collapsed-anchor mechanism was wrong (it is a
taken-over prose block, or an open fold), and #PPR4 item 2 did not flatten the per-turn curve —
the walks are now 0.07 % and 0.11 % of GUI cycles, and the growth is elsewhere. Moved to
needs-verification with a four-line checklist: nothing should look different.
