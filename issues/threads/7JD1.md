<!-- relay:entry 20260921T230213Z-2m author=agent kind=event pane=switchboard -->
- ✦ agent created this card in Planned · issues/features/2026-09-21-stop-pauses-the-queue-enter-resumes-it-on-the-pa.md

<!-- relay:entry 20260921T230213Z-y6 author=agent kind=decision pane=switchboard -->
Owner, 2026-09-21: "why don't we just copy the functionality and have enter resume" — taken as the design: Stop pauses the queue, Enter resumes it, on the pane and on a phone alike; the one-queued-item/return-to-box alternative is set aside. Runs after #CTRN's final pass.

<!-- relay:entry 20260922T001631Z-ek author=agent kind=progress -->
Claimed by the `enter-resumes` session (Claude Opus 5, subagent). Working the three steps in order: the worker first, then the pane and a card's console, then the remote protocol.

**The wire choice, made in step 1 and landed first** (`2d2993e1`): the **worker** resumes on the submit, not the pane sending `resume_queue` after its `ask`. The card left it open; this is the side that leaves a terminal pane's wire byte-for-byte unchanged *whatever* the queue is doing, not only when nothing is paused — the pane sends nothing new in either case — and it is the same code that makes a device's `ask`/`board_ask` resume, which step 2 asks for anyway. `TurnSupervisor.submit` clears `_paused` on its way past, after the `busy` check that reads it, for everything except `origin: "relay"` (a background subagent reporting back must not undo somebody's Stop).

<!-- relay:entry 20260922T005810Z-9j author=agent kind=note pane=switchboard -->
Handoff: IN PROGRESS at handoff time — a subagent was mid-way (worker resume-on-submit landed; remote/device resume tests were being written when the session paused). Re-check `python3 scripts/land.py who` for a live `enter-resumes` claim before starting fresh; if idle/stale, resume from its dry-run or just re-launch the step. Design is settled (owner, 2026-09-21): Stop pauses the queue, Enter resumes it, same on pane/console/phone; the one-item/return-to-box alternative was explicitly rejected, do not revisit it.

<!-- relay:entry 20260922T010630Z-vc author=agent kind=progress -->
**All three steps are landed; the live drive is not finished.** Handing over at a clean point — the session was stopped, and the card stays in `executing` rather than moving to `needs-verification`, because the pane/console drive has not been run green yet.

Commits: `2d2993e1` (the worker resumes on a submit), `69260659` (the pane and a card's console: Enter on an empty box; the strip's hint reads "Enter resumes"), `b8582fea` (`board_resume {id}`, the eleventh request of REMOTE-PROTOCOL 17.1, and 17.4's "waits for the desktop" is gone), `7d36ec3a` (`queue_resume {pane}` for the pane view). Protocol 12.5 (v4.7), 19.16, REMOTE-PROTOCOL 16/17.1/17.3/17.4 and REMOTE-AND-MULTIPLAYER-DESIGN 12.8 carry the rule.

**The wire choice**, which the card left open: the worker resumes on the submit rather than the pane sending `resume_queue` after its `ask`. It leaves a terminal pane's wire byte-for-byte unchanged *whatever* the queue is doing, and it is the same code that makes a device's `ask`/`board_ask` resume.

**The ten-shot terminal-pane pixel gate is done** and is in `docs/qa_evidence/2026-09-21-enter-resumes-the-queue/` (`term-before/` at `d6682f97`, `term-after/` at `7d36ec3a`, both 6 PASS / 0 FAIL with identical notes). Five shots are 0 pixels; 04 and 07 are the spinner frame, the turn clock and the caret; **08 and 09 are 2131 pixels of this card's own change** — the paused strip's hint — and 01's 21310 are a subagents status line whose source is byte-identical between the two tips, so it is a timing difference between two runs. NOTES.md reads every number.

**What is left**, for whoever picks this up: `drive.sh` and `stub-provider.py` in that directory are written and were run once. The first run failed four checks and the failures were the harness's: the stub logged only the first 400 characters of each user message, and Relay prefixes a prompt with its context block, so the typed words — which are at the *end* — were never seen, the slow scene was never picked, and there was no running turn to queue behind. That is fixed in the stub here and has **not been re-run**. Run `drive.sh <relay> <out>` on a build of the landed tip for all three phases (`empty`, `typed`, `card`), drive a device session, then move this card to `needs-verification` with that path.
