<!-- relay:entry 20260919T182918Z-hb author=agent kind=decision -->
### Fable 5.1 · 2026-09-19 18:29
Rewrote Decisions and Tasks: the owner dropped the personal inbox board and the git-repo-backup
proposal that rode with it (2026-09-19). Replaced with `relay::projects::defaultProject()`, an
optional "Default project for loose cards" setting, landed in `5c329706d63f` alongside the
hidden-board-folder decision's C++ half — both touched `src/Projects.h`/`.cpp` the same day. The
"Move to project…" task is superseded (no inbox card left to move). Picker, tab chip, removable
known-projects list and project grouping stay open GUI tasks, unchanged.

<!-- relay:entry 20260919T200500Z-pk author=agent kind=progress model=claude-fable-5-1 commit=28f2c27 -->
Decisions 1–5 implemented (owner, 2026-09-19). 1: `src/ProjectPicker.{h,cpp}` (relay-projectpicker), a pane
hosted like the ⓘ view with `paneType` "projects"; opened by Ctrl+Shift+S / `/card` / the palette in a tab with
no project and no candidate; "Initialize new project here" on top attaches the tab to the pane's directory and
sends `board_init {git_init: true}` — the choice is the consent, so no second question; the worker runs `git init`
only outside a repository and reports what it did on the answering `board_state` (`git`). Landed as 28f2c27.
2: the chip in the tab's left box beside the ⧉ button (`syncTabProjectChip`); the palette detach is kept as the
keyboard path and the chip's click teaches it. 3: Options › Agent › Switchboard lists known projects (reason, age,
Remove → `Registry::forget`) and declined ones (Undo → `undecline`). 4: the Switchboard's gear page carries
"Hide/Show this board's folder" (`board_folder`, 19.17); `board_folder_changed` re-roots the view and reloads.
5: a Project chooser in the Sessions pane, asked of the worker as `project` / `outside_projects` (14.3, the index
answers with an equality-or-prefix test on the canonical workspace). 6 (grouping) is split out to #TVE1.

<!-- relay:entry 20260919T203500Z-q7 author=agent kind=progress model=claude-fable-5-1 commit=604e01c7 -->
Landed: 28f2c27 (picker, `board_init {git_init}`), 7e3fb9f (tab chip, Options list), 5ee7259 (gear-page folder action),
8e02fca (Sessions Project chooser, 14.3 `project` / `outside_projects`), 6c68a2a (docs, #TVE1, BOARD.md, the old card
path removed) and 604e01c7 (the card in needs_qa_llm/ with its checklist, and the evidence — land.py's `--whole` paths only
land when they are also in `--paths`, which 6c68a2a missed). Evidence: docs/qa_evidence/2026-09-19-project-picker/.
