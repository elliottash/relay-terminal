ctrl+i toggle between agent and terminal
when you open an interactive program like vim / codex, the default is to go into human control mode, the prompt box disappears. and it gives a notification, ctrl+H to bring the terminal back 

to discuss -- i think ctrl+I should toggle terminal command vs agent prompt. ctrl+D should toggle human action vs agent delegation into non-terminal programs / active jobs.
should we make it that ctrl D or ctrl H takes human control and removes the relay prompt, and ctrl shift D or ctrl shift H gets you back to the relay prompt.
highlight text copies. 

parse all folders and filenames and highlight them. 

clicking a directory or the terminal working directory opens a dolphin-like file explorer pane with the folder.

clicking a previewable file opens it in a pane. 

make processes pane-specific, so if something blows up with memory, it doesnt crash the full terminal 

 
