cmake_minimum_required(VERSION 3.22)
project(RelayMacOSSmoke LANGUAGES CXX)
if(NOT APPLE)
  message(FATAL_ERROR "Run native macOS smoke tests on macOS")
endif()
set(CMAKE_CXX_STANDARD 17)
find_package(Qt6 6.4 REQUIRED COMPONENTS Core)
enable_testing()
include(platform-smoke.cmake)
